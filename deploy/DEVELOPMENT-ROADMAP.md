# 診断機完成までの開発計画

確認日: 2026-09-09。ローカル実装対象: 3.13.553。公開確認済み: 3.13.553。計画改訂時の基準版: 3.13.436 / 63510896。

3.13.553 公開確認: 012c4ec9を承認済みGitHub mainへ送信。公開主要5経路のHTTP 200、script/SW/manifestの553一致、未完了FileReader中止処理を照合した。

3.13.553 回帰検証: R1全工程、端末起動1,329項目がエラー0。隔離Chromeのオフライン取込・出力・再取込・密ライブ推移・異常復旧一巡も合格。配布ZIPは2,662,766 bytes / SHA256 67BBC2772B4586F9C3B430E8610D61D73A055E73BF76AAB569230C56A0BE6B2C。別フォルダー展開後の検査コマンドで846対象ファイル / 16,887,057 bytes一致、終了コード0。実VCI・実車・別PC・OS再起動の確認ではない。

3.13.553 診断結果ファイルの取消時に読込を中止: 従来は取込操作の所有権だけを失効させ、背景のFileReader読込を止めていなかった。operationへreaderを保持し、無効化では先に所有権を失効してからLOADINGのreaderだけabortする。新しい取込開始も旧読込を無効化する。同期onabort、abort例外、遅着onload/onerrorを無視し、完了済みreaderとclipboard操作にはabortしない。単体で修正前の中止欠落を再現し、取消・差し替え・例外・遅着・新規取込復帰が合格、既存navigation/R1へ統合。隔離Chromeの実FileReaderで未完了1→完了2とabortイベント1回を確認し、既存結果・状態表示・JSON保存操作が保持されることを照合した。ブラウザー検証スキルの代替として既存Playwrightを使用。自動再試行・車両送信・結果契約・保存形式・取込上限は変更していない。公開は別途確認する。

3.13.552 公開確認: 4349d876を承認済みGitHub mainへ送信。公開主要5経路でHTTP 200、script/SW/manifestの552一致、script.jsの再読込失敗時の類似事例更新を照合した。

3.13.552 配布整合性: ZIP 2,662,632 bytes / SHA256 E6B9B043FD189E412D496E22B448AE77EC2A0FCEEBFF449300DF7F7590D9B424。別フォルダー全展開後のverify-workstation.cmd --no-pauseで846対象ファイル / 16,886,709 bytes一致、終了コード0。実車・別PCの確認ではない。

3.13.552 保存不整合時の古い類似事例表示を解消: reloadSavedCasesの読込失敗とpersistCasesの別タブ変更検知で共通警告だけを更新し、古い類似事例カードが残っていた。両経路で既存renderSimilarCasesを呼び、既存の未読込/未確認表示へ切り替える。保存文字列・メモリー上の事例・類似度計算・診断契約・車両通信は変更しない。修正前の単体で残存経路を再現し、事例344項目とR1全工程合格。ブラウザー検証スキルの代替の隔離ChromeでP0171の類似事例1件を表示後、別タブの実取込と古いページの保存競合検知により0カード/未確認表示へ変わり、手動再読込で1件へ戻ることを確認した。既存保存復元・CSV・大量取込・特殊ID・入力クリア保護も合格。390幅の共通警告を目視確認。公開は別途確認する。

3.13.551 公開確認: 33eec33fを承認済みGitHub mainへ送信。公開主要5経路のHTTP 200、script/SW/manifestの551一致、script.jsのクリア前確認を照合した。

3.13.551 配布整合性: ZIP 2,662,635 bytes / SHA256 222508C959967CFE95AE980EDEB02376687BEB590646C452746C251436847582。別フォルダー全展開後のverify-workstation.cmd --no-pauseで846対象ファイル / 16,886,645 bytes一致、終了コード0。署名・実車適合・別PCでの起動確認とは区別する。

3.13.551 事例入力クリアの誤操作保護: クリアボタンが確認なしで入力を失わせることを単体で再現し、未保存入力は元に戻せないこと・保存済み事例は削除しないことを明示した確認を追加。取消時はフォームと品質表示を保持、実行時だけ既存の初期化を行い、双方の結果を画面に表示する。事例単体334項目とR1全工程合格。ブラウザー検証スキルの代替の隔離Chromeで実確認ダイアログの取消/実行、全入力値の取消前後一致、実行時の入力消去・日付/ID初期化・保存文字列不変を確認し、390幅の取消表示を目視確認した。既存のJSON/CSV保存復元、大量取込、特殊ID削除、別タブ変更検知も合格。入力の自動保存・ページ終了時保護・保存形式・診断契約・車両通信は変更しない。公開は別途確認する。

3.13.550 CI確認: 完全コミットID 30bffdba6796903a95dfcf872bc1109bb2fd43f4 に対するGitHub Actions「Deploy Read-Only Validation」実行34285375425はcompleted/success。短縮IDによる検索は結果なしだったため、完全IDで照合した。CI成功を実車適合の確認とは扱わない。

3.13.550 配布物の起動・更新一巡を完了: 未変更のZIP展開物を隔離Chromeで実起動し、初回注意、誤パスワード拒否、全オフライン資材保存、両サーバー停止後のブラウザー終了・再起動、キャッシュ画面復元とロック保持が合格。再起動直後の補助CDPキャッシュ一覧はNo frameだったが、実ナビゲーションの復元と容量保持で別途確認した。続けて同一originで548→550→548を実行し、更新資材だけのHTTP 503注入で不完全更新の拒否、旧完全キャッシュと模擬事例の保持、手動操作相当の正常更新と旧版復帰後の保存文字列不変を確認。未同梱faviconの失敗2件のみ別集計。ブラウザー検証スキルの代替として既存Playwrightを使用し、390幅の失敗案内を目視確認した。ZIPのSHA256 7F4CD341814C2817991C95D98E7C7F792251A1666990B93613E8873959457B93は不変。実パスワードでの成功・別PC・OS再起動・実VCI/実車は未確認。アプリの追加変更やZIP再生成はしていない。

3.13.550 公開確認: 30bffdbaを承認済みGitHub mainへ送信し、公開主要5経路のHTTP 200、script/SW/manifestの550一致、script.jsの保存文字列比較処理を確認した。公開HTTPはローカル回帰検査や実車検証とは別の確認として扱う。

3.13.550 配布ZIP: 2,662,434 bytes / SHA256 7F4CD341814C2817991C95D98E7C7F792251A1666990B93613E8873959457B93。全展開後のverify-workstation.cmd --no-pauseで846対象ファイル / 16,886,228 bytes一致、終了コード0。複製整合性だけの確認であり、実車・別PC・OS再起動は未確認のまま維持する。

3.13.550 整備事例の古い一覧からの上書きを抑止: loadCases成功時の保存文字列をメモリー内に保持し、persistCasesで直前の保存文字列と照合する。別タブの追加/削除/不正文字列などへの変更があれば書込を中止し、共通警告と手動再読込を案内。正常保存・明示的全削除の成功時だけ比較用文字列を更新する。正規化で補われる日時等は比較せず、保存形式は変更しない。修正前の単体検査で上書きを再現し、修正後は事例保存324項目合格。読み取り拒否時の無書込、連続保存、削除後保存、手動復帰を含む。R1全工程合格後、追加4項目を含む事例単体も再確認。隔離Chromeの同一originの2ページで、一方の実JSON取込後に古いページで取込すると保存文字列と入力フォームが保持されること、再読込後の再取込と画面再起動で両方の事例が残ることを確認。既存の大量取込・特殊ID・選択削除・CSV・JSON復元も合格し、390幅の警告を目視確認した。ブラウザー検証スキルの代替として既存Playwrightを使用。localStorageの比較と書込は原子的ではなく同時書込ロックではないため、編集は1タブを案内する。自動マージ・自動再保存・診断結果の契約・実車通信・接続権限は変更していない。公開は別途確認。

3.13.549 公開確認: 854cbfb0を指定の公開GitHub mainへ送信し、公開主要5経路と/style.cssの計6経路でHTTP 200、script/SW/manifestの版549一致、CSSの警告保持セレクターを確認。ローカル回帰検査・配布整合性・公開HTTPを区別して記録した。既存30分間隔の継続実行はACTIVEで、追加の自動実行は作成していない。

3.13.549 配布整合性: ZIP 2,662,193 bytes / SHA256 B1E00E7B5B0B8CA71F2A8E646CF7D2B0DA3E8589EF0DE08614119A53AC08553F。別フォルダーへの全展開後、verify-workstation.cmd --no-pauseで846対象ファイル / 16,885,409 bytes一致、終了コード0。複製の整合性だけの確認であり、署名・実車互換性・送信許可は証明しない。

3.13.549 読取結果の印刷で読込失敗警告を保持: 印刷用CSSが結果領域外の共通警告まで隠していた問題を、修正前の隔離Chrome検査で再現した。印刷時の除外対象からstaticDataWarningだけを外し、白背景・黒文字で印字する。hidden属性は変更せず、正常時は非表示のまま。診断結果の契約・保存形式・車両通信には変更なし。既存印刷ブラウザー検査へ、通常非表示/失敗警告表示と結果選択保持を追加し、全体結果・ライブ未取得・追加データ選択・DTC絞り込みで通過。390幅の印刷用表示を目視確認した。R1全工程と隔離Chromeのオフライン読込・出力・再取込・密ライブ推移・異常復旧の一巡が合格。指定CLIがないためブラウザー検証スキルの代替として既存Playwrightを使用。実プリンター・OS印刷ダイアログ・別PC・実VCI/実車は未検証。公開反映は別途確認する。

2026-09-09 / 3.13.548 公開保留を解消: 公開GitHub southwind1363-bot/vehicle-diagnosis-toolのmainと対象8ファイルを明示した確認への利用者の「続けて」を受け、61119c53を送信した。公開主要5経路（/、/script.js、/obd-readonly.js、/service-worker.js、/offline-assets.json）のHTTP 200、3か所の版3.13.548、HTMLの共通警告とscript.jsの失敗時表示処理を確認。既存ZIPのSHA256 E2358425413205307B4C23336CED76BA7052D0B1483F0D5B5E57E79033B52B20も不変。以下の公開承認待ち・保留記述は各作業時点の履歴であり、この確認で解消した。実車・別PC未確認の範囲は変わらない。

2026-09-09 / 3.13.548 総合検証完了（公開承認待ち）: 未実施だったvalidate:releaseを1回実行し、全工程が終了コード0で完了した。OBD主集計7,447、bridge384、J2534 lifecycle242/supervisor154、端末起動1,329、配布396、offline180、データ203 JSON/3,878 DTCはいずれもエラー0（データ警告0）。静的JSON期限・共通読込警告・事例検索/保存/CSV・結果印刷の追加検査も既存経路で通過した。長時間の境界条件検査を待ったもので、同じ失敗を繰り返したものではない。新たなアプリ修正や配布ZIP再生成、commit/push、実利用者データ・実車操作は行わず、記録のみローカルに保持。公開には先に求めた承認が引き続き必要で、実VCI/実車・別PC確認の代替にはしない。

3.13.548 公開承認待ちの配布確認: 拒否されたcommit/pushは再試行せず、未変更のZIP展開物を隔離Chromeで実起動した。初回注意、誤パスワード拒否、オフライン資材保存、両サーバー停止後のブラウザー終了・再起動、通信遮断下の画面復元とロック保持が合格。390幅も確認。続いて同一originで3.13.545→3.13.548→3.13.545を検証し、更新資材のHTTP 503を1回注入した不完全更新の拒否、旧版と模擬事例の保持、正常更新、旧版復帰後の保存文字列不変が合格。起動直後の補助CDP一覧取得失敗は実ナビゲーションでの復元と容量保持により別扱い、未同梱faviconの失敗だけを別集計した。ブラウザー検証スキルの代替として既存Playwrightを使用。既存ZIPのSHA256 E2358425413205307B4C23336CED76BA7052D0B1483F0D5B5E57E79033B52B20は不変。実パスワード・実VCI/実車・別PC・OS再起動は未確認。結果はローカル記録のみで公開保留は継続する。

3.13.548 配布検査・公開保留: ZIPは2,662,173 bytes、SHA256 E2358425413205307B4C23336CED76BA7052D0B1483F0D5B5E57E79033B52B20。展開物の整合性検査を実施。公開GitHub mainへの対象8ファイルのcommit/pushは自動審査で拒否され、迂回・再送信はしていない。変更はローカルに保持し、この版の公開反映は未確認。外部公開先と変更内容を明示して利用者の判断を求める。

3.13.548 静的整備データの読込失敗を全タブへ通知: 従来のdataStatusは診断補助タブ内にあり、車両読取中に内蔵サンプルへ切り替わっても見えなかった。既存の失敗処理と成功処理から全タブ共通のrole=alert警告を表示/非表示にする。最新登録データとして扱わないことと、必要な結果・事例を保存してから通信環境確認と手動再読込を行うことを案内する。自動再試行や再読込ボタンは追加せず、判定・通信・保存形式は変更しない。単体で通常非表示/失敗表示/成功時解除を検査しR1全工程合格。隔離Chromeで実30秒の静的JSON応答停止後、旧dataStatusが車両読取タブで非表示であること、共通警告は車両読取/データ管理/事例検索で表示されること、390幅での表示、認証ロックと保存文字列保持を確認。指定CLIがないためブラウザー検証スキルの代替として既存Playwright検査を使用。実データ・実パスワード・VCI/実車は不使用。公開反映は別途確認する。

3.13.547 公開・配布物確認: ea786a13をorigin/mainへ反映後、公開5経路（/、/script.js、/obd-readonly.js、/service-worker.js、/offline-assets.json）のHTTP 200、3か所の版3.13.547、静的JSONの30秒期限処理を確認。ZIPは2,662,007 bytes、SHA256 EF4CFF7A4E5338A9659CC2C003B92B6323C298FD0C97D2644DBA91D47F9E577E。展開先のverify-workstation.cmd --no-pauseは846ファイル / 16,884,755 bytes一致、終了コード0。複製整合性の確認であり、実車互換性・署名・送信許可の証明ではない。

3.13.547 静的診断データの無期限待機を解消: loadData専用のfetchJsonに30秒のAbortController期限を設定。ヘッダー受信だけでなくresponse.jsonの完了まで期限を維持し、成功・HTTP失敗・解析失敗・通信失敗のいずれもタイマーを解除する。応答停止時には取得を中止して既存の内蔵サンプル/JSON読込不可表示へ移る。車両通信・接続権限・自動再試行・診断結果の契約・保存形式は変更しない。新規単体30項目は修正前の期限欠落を検出し、成功/HTTP/解析/通信/ヘッダー待機/本文待機と1回だけの取得を確認、既存navigation/R1経由で検査する。R1全工程合格。隔離Chromeでは静的JSONの1件だけを応答停止させ、実30秒後の既存失敗表示、対応確認中の終了、認証ロック、保存文字列保持、要求1回を確認。指定CLIがないためブラウザー検証スキルの代替として既存Playwright検査の--data-timeoutを使用。実データ・実パスワード・VCI/実車は不使用。公開反映は別途確認する。

3.13.546 公開・配布物確認: 7c684059をorigin/mainへ反映後、公開5経路（/、/script.js、/obd-readonly.js、/service-worker.js、/offline-assets.json）のHTTP 200、3か所の版3.13.546、公開script.jsの複数語AND照合を確認。ZIPは2,661,892 bytes、SHA256 792E219B08B3A160B254D55F2B2816632108370DFC0030FA79AE236109CDC00B。展開先のverify-workstation.cmd --no-pauseは846ファイル / 16,884,473 bytes一致、終了コード0。複製整合性の確認であり、実車互換性・署名・送信許可の証明ではない。

3.13.546 整備事例の検索を改善: 入力例「プリウス P0171 アイドル」が、全入力を1つの連続文字列として照合していたため一致しない問題を単体検査で再現。空白区切りの各語が同じ事例内にすべて含まれるAND検索へ変更し、全角空白・大文字小文字・語順差を許容する。事例ID、作業内容、交換部品、メモも検索対象へ追加。「検索条件に一致なし」と「保存事例なし」「読込失敗」を区別し、検索ラベルに空白区切りの絞込みを明記した。保存形式・保存レコード・診断判定・類似事例の採点は変更しない。単体23項目を既存case-storage/R1/OBD経由に統合し、R1全工程合格。隔離Chromeで復元後の複数語/全角空白/ID/作業内容検索、該当なし、解除後全件表示、保存文字列不変、390/1280幅を確認した。既存の大量取込・特殊ID・選択削除・CSV再試行も合格。ブラウザー検証スキルの指定CLIがないため既存Playwright検査を使用し、実利用者データ・実VCI/実車は不使用。公開反映は別途確認する。

3.13.545 配布版の起動・更新を確認: 未変更のZIP展開物を実ローカルサーバーで起動し、隔離Chromeで初回注意、誤パスワード拒否、全オフライン資材保存を確認。Web/bridge両サーバーを停止し、ブラウザーを終了・再起動して通信を遮断した状態でも画面を復元し、認証ロックを維持した。390幅の表示も確認した。次に3.13.541→3.13.545→3.13.541を同じoriginで切り替え、更新資材へのHTTP 503を2回注入。不完全な更新の拒否、旧キャッシュと模擬保存事例の保持、正常配信へ戻した後の更新、旧版へ戻した後の保存文字列不変が合格。補助CDPキャッシュ一覧は起動直後にNo frame foundとなるが、実ナビゲーションとキャッシュ容量保持で復元を確認済み。更新検査の未同梱favicon要求失敗のみ別集計し、アプリのエラーはなし。ブラウザー検証スキルの指定CLIがないため既存Playwright検査を使用。実パスワード・実車・別PC・OS再起動は未確認。既存ZIPは再生成せず、SHA256 8A4DE42D3DA587E53B717767A618C3AEFBA4D3A9D271E110536F24CE819D8B9Eを保持。

3.13.545 GitHub検査の確定: a2b5a449の実行34222441505をGitHub APIで確認し、診断データ/読取専用契約とChromiumブラウザー一巡の両ジョブがcompleted/success。前段の「GitHub側未確認」はその時点の記録であり、この確認で解消した。

3.13.545 継続ブラウザー検査の拡充: 既存CIが実行するvalidate:scanner-browserに整備事例の--bulk --unsafe-id --delete-identity --csvを連結し、診断結果のオフライン一巡に加えて大量取込・特殊ID・対象1件削除・CSV復旧を毎回検査する。事例スクリプトもPLAYWRIGHT_CHANNELを受け付け、スクリーンショット用の一時フォルダー名を既存CIのPNG収集対象に合わせた。バックアップJSON/CSVや実利用者データは収集しない。ローカルのChromium実行は未導入の実行ファイル不足で開始できず、導入済みChromeで連結コマンド全工程が終了コード0。模擬VCIの切断・失敗・ゼロDTC・無応答からの再読取/保存/オフライン復元、1,000件保持、特殊ID、選択削除、CSV再試行も合格。ブラウザー検証スキルの指定CLIがないため既存Playwright検査を使用。端末向け検査1,329項目も合格。GitHub側の今回の実行結果は未確認であり、公開機能・保存形式・アプリ版・既存配布ZIPは変更しない。

3.13.545 公開・配布物確認: R1全工程合格後、c7e05a5bをorigin/mainへ反映。公開5経路（/、/script.js、/obd-readonly.js、/service-worker.js、/offline-assets.json）のHTTP 200、3か所の版3.13.545、公開script.jsのCSV失敗案内を確認。ZIPは2,661,780 bytes、SHA256 8A4DE42D3DA587E53B717767A618C3AEFBA4D3A9D271E110536F24CE819D8B9E。展開先のverify-workstation.cmd --no-pauseは846ファイル / 16,884,129 bytes一致、終了コード0。複製整合性の確認であり、実車互換性・署名・送信許可の証明ではない。

3.13.545 CSV保存の失敗案内と再試行: CSV出力にも既存JSON出力と同様の例外処理・状態表示・一時リンク除去・遅延URL解放を適用。保存開始と完了を区別し、生成やダウンロード開始に失敗した場合は非公開の例外詳細を表示せず再試行を案内する。既存CSV列・BOM・引用符処理・ファイル名・空事例と読込エラー時の停止は維持。追加した単体83項目で各生成段階の失敗・再試行・元データ保持・URL解放を確認。隔離ChromeでURL生成失敗を注入し、再試行した実ダウンロードのBOMを含む全内容と既存CSV生成結果の一致、保存データ不変、画面表示を確認した。ブラウザー検査は指定CLIがないため既存Playwrightスクリプトを利用。実利用者データ・実VCI・実車は不使用。公開反映は別途確認する。

3.13.544 公開・配布物確認: be7d3031をorigin/mainへ反映後、公開5経路（/、/script.js、/obd-readonly.js、/service-worker.js、/offline-assets.json）のHTTP 200、3か所の版3.13.544、公開script.jsのレコード参照による対象選択を確認。ZIPは2,661,753 bytes、SHA256 DC11760A6F56ECB36D344613FCC6B3A20A3FE9C8EC794E14C432A20F6CB9B374。展開先でverify-workstation.cmd --no-pauseが846ファイル / 16,883,357 bytes一致、終了コード0。複製整合性の検査であり、実車互換性・署名・送信許可の証明ではない。

3.13.544 事例削除の対象を1件に限定: 従来はHTML属性の文字列IDと保存IDを比較していたため、数値17のカード操作で別の文字列ID「17」の事例が消える不具合を単体検査で再現。描画時のボタンと元レコードをWeakMapで関連付け、選択されたレコードの位置1件だけを保存対象から除くよう修正した。同一IDの別レコードは保持し、古いボタンは保存を書き換えない。ID・保存形式・取込の重複判定は変更しない。数値/文字列・重複文字列・真偽値・オブジェクトID、取消、古いボタンを含むカード検査32、JSON出力72、保存301項目が合格。R1全工程も合格（当時カード24項目、追加した取消8項目はその後保存検査で合格）。隔離Chromeで既存の取込/バックアップ/復元/特殊ID一巡に加え、数値IDと同一IDの選択削除、他事例保持、再読込後の保持を確認。初回ブラウザー検査で検出した初期化順の問題も修正し、再実行で合格。実利用者データ・実VCI・実車操作は不使用。公開反映は別途確認する。

3.13.543 公開・配布物確認: 29f536c3をorigin/mainへ反映後、公開URLの /、/script.js、/obd-readonly.js、/service-worker.js、/offline-assets.json がHTTP 200であること、3か所の版が3.13.543であること、配信script.jsに削除対象IDのescapeHtml適用が含まれることを確認。ZIP展開先でverify-workstation.cmd --no-pauseを実行し、846ファイル / 16,883,134 bytesの一致と終了コード0を確認した。これは複製整合性の確認であり、署名・実車互換性・送信許可の証明ではない。
この文書は開発者用です。通常診断画面へ表示する進捗データではありません。

## 現在地とR1の出口

3.13.543 事例IDのHTML属性を安全に表示: 事例カードの本文はescapeHtmlを通す一方、削除ボタンのdata-delete-case属性だけが取込IDを直接展開していた。引用符と不活性なspanマークアップを含む模擬IDで、修正前に入力がHTML要素になることを隔離Chromeで再現。属性にも既存escapeHtmlを適用し、保存IDの文字列・保存形式は変更しない。単体16項目をcase-storage経由でR1/OBD検査へ統合。ブラウザーでは同じJSONを取り込み、要素注入なし、IDの文字列表示、datasetの元ID一致、明示確認した模擬事例だけの削除と他の模擬事例保持を確認。1,000件の保存/再取込を含む既存一巡とR1全工程も合格。実パスワード・利用者データ・実VCI/実車は不使用。実行コードを含まない試験用マークアップだけを使用した。ZIPは2,661,676 bytes、SHA256 ED2F6AE72DA646EA8C3FB02AD1B1AE225AF5CBB2BC8414039EEDCBB47BFEEC22。公開反映は別途確認する。

3.13.542 総合検証の累積確認: 印刷検査53項目がR1だけに入っていたため、既存のOBD検査からも呼び出し、総合検証とGitHubの既存OBDジョブに含めた。validate:releaseはOBD主集計7447/Errors 0、bridge384、J2534 lifecycle242/supervisor154まで合格し、PC起動検査のnpmコマンド一覧照合で停止した。原因は538でR1へ追加した印刷検査を期待値が含んでいなかったこと。実コマンドと整合するようその期待値だけを更新し、停止工程から再開してPC起動1328、配布396、offline180、データ203 JSON/3878 DTC（Errors 0/Warnings 0）に合格した。総合コマンドを最初から2回実行した意味ではなく、前半の合格と修正後の後半合格を合わせた確認。アプリ本体・保存形式・車両通信・版番号は変更しない。既存542 ZIPのSHA256 A049C40F68762C3FFFD7AD5D8189B0AE96FB83BD23C4FF4258A1B6F7EA47FA3Fも不変。実車・別PCの確認とは分けて扱う。

3.13.542 公開保留を解消: 公開GitHub southwind1363-bot/vehicle-diagnosis-toolのmainへ未公開4コミットを送信する旨を明示して確認し、利用者の「続けて」を受けて送信した。mainはffd968e1からd952e401へ更新。公開主要5資材のHTTP 200、script/service-worker/offline-assetsの3か所で542一致、保存状態欄と一括取込索引処理の配信を確認した。以下の540～542にある公開保留・ローカルのみの記述は、その作業時点の履歴。既存542 ZIPは再生成せず、SHA256と内容を保持する。実VCI/実車・別PCでの未検証は解消した意味ではない。

3.13.542 整備事例の一括取込照合を改善（ローカルのみ）: 各入力行で取込途中の全事例へfind/someを繰り返す処理を、既存のduplicateKeyとIDのSetを取込ごとに1回作る方式へ変更。採用済みの行だけを索引へ追加し、先に採用した事例・配列順・件数・保存形式・既存の正規化基準を保持する。既存1件+新規300件のキー計算は修正前45,450回（回帰失敗を確認）から301回以下へ削減。12組の数値/文字列ID・同一ID・正規化した内容・入力内重複・不正行を旧アルゴリズムで算出した全レコード/順序/件数と照合して一致。case-storage301項目、JSON保存72項目、R1全工程合格。隔離Chromeで1,100行（うち同じ入力内の重複100行）を選択し、1,000件採用・キー計算1,100回を確認。実測242msはこのPCの模擬入力の観測値で速度保証ではない。1,000件のJSON実ダウンロードと端末保存文字列の内容一致、再取込で1,000件スキップ・保存文字列不変を確認し、画面を目視確認した。ZIPは2,661,679 bytes、SHA256 A049C40F68762C3FFFD7AD5D8189B0AE96FB83BD23C4FF4258A1B6F7EA47FA3F。別フォルダーへ全展開した配布コマンドで846対象ファイル/16,883,122 bytes一致。実データ・実パスワード・VCI/実車は不使用。公開拒否への再試行はせず、外部反映は承認待ちのまま。

3.13.541 配布版の起動・更新確認（ローカルのみ）: ZIP展開済みの未変更パッケージを実ローカルサーバーで起動し、初回注意、誤パスワードの拒否、全オフライン資材保存、両サーバー停止後のブラウザー再起動・ネットワーク無効化とService Workerからの復元、認証ロック維持を確認した。続いて539→541→539を同一originで切り替え、模擬事例1件の保存文字列とロック状態を保持。541更新資材への試験専用HTTP 503を2回観測し、不完全更新の拒否と旧キャッシュ/保存事例維持、健全なサーバーへ戻した後の更新・旧版復帰に合格。390幅の初回注意・再起動後ロック・更新失敗通知を目視確認。両パッケージは試験前後で整合一致し、541 ZIPのSHA256も不変。ブラウザーの未宣言favicon要求によるERR_FAILED/404は別記録し、アプリ例外と予期しない外部要求は0。ブラウザー再起動直後・ページ作成前の補助CDPキャッシュ一覧ではNo frame foundが出たが、その後の実ページでworker応答と全資材を検証済み。同一PC/一時プロファイル/模擬事例に限る検査であり、OS再起動・別PC・実パスワード・実VCI/実車は未検証。アプリ本体・版・ZIPは変更せず、外部pushも再試行していない。試験記録: OS一時フォルダーのpackaged-browser-PlAhmP / packaged-update-swnuxi。

3.13.541 JSON取込の例外表示を固定化（ローカルのみ）: 取込処理のcatchがerror.messageをそのまま表示しており、エンジンのJSON例外に含まれる入力断片を表示し得た。従来の「private-file-detail全文が含まれない」検査は省略された断片まで確認できないため、入力断片を含む例外を模擬する回帰を追加し、修正前の失敗を確認した。例外本文を使わない日本語の形式/内容確認と再選択案内へ変更。保存後の画面更新エラーも同じcatchへ到達し得るため、全失敗で「保存内容は不変」とは断定せず、保存済み一覧の確認を案内する。取込判定・保存形式・データの反映順は変更しない。case-storage275項目（JSON保存72項目併行）とR1全工程合格。隔離Chromeで模擬の不正JSONを実際に選び、入力断片・英語例外が表示されないこと、正しいJSONへの再選択・保存・再取込と既存の遅延通知検査に合格し、390幅の案内を目視確認。利用者のデータ・実パスワード・実VCI/実車は使用しない。ZIPは2,661,555 bytes、SHA256 7C2A5678D83576D40DF6E758D09F48F2AD38BF6CABE5EC5690B914453F02E007。別フォルダーに展開した配布検査コマンドで846対象ファイル/16,882,851 bytes一致。540での公開拒否後に新しい承認はないため外部pushを再試行せず、公開は承認待ちとしてREADMEにも明記した。

3.13.540 公開保留: 2026-09-08の自動継続実行で、コミット/pushを含むコマンドが実行前の自動審査で拒否された。理由は公開GitHubのmainへの送信について宛先・mainの明示承認がないとの判定。拒否を迂回せず、公開済み版は539のままとして扱う。ローカル検証と配布ZIPは完成しているが、540の公開確認済みとはしない。

3.13.540 整備事例JSON保存失敗からの復帰: 従来のJSONバックアップは生成/ダウンロード開始の例外を捕捉せず、利用者への失敗通知がなかった。データ管理画面へ専用の保存状態欄を追加し、開始と完了を区別、開始失敗時は元データを保持して再操作を案内する。リンクは一時的にDOMへ付けて終了時に除去し、Blob URLはクリック中に即時破棄せず次のタスクで解放する。保存データの構造・整形・名前・0件の出力可否と、保存領域の読込失敗時の既存拒否は変更しない。生成/JSON直列化/Blob/要素/URL/追加/クリックの例外、元データ保持、URL/リンクの後処理と秘密値非表示を72項目で検査しcase-storageへ統合。R1全工程合格。隔離Chromeの模擬事例1件でURL生成失敗の画面案内と保持、復帰後の実ダウンロード内容一致、再読込/再取込の重複スキップ・保存文字列不変、既存の取込失敗/遅延通知検査が合格。390幅の失敗表示を目視確認。ブラウザーの保存完了・取消をアプリ側で検知した意味ではなく、既知の自動ブラウザー再起動後障害の修復とも扱わない。ZIPは2,661,441 bytes、SHA256 BE1A25F471E4A751FAD8EE85C83530F538178E543B48C9EE92935E8A10FCFA0B。別フォルダー展開後の配布検査コマンドで846対象ファイル/16,882,565 bytes一致。実データ・実パスワード・VCI/実車は使っていない。公開反映は別途確認する。

3.13.539 公開確認: f3858da8をGitHub mainへ送信後、公開主要5資材のHTTP 200、版情報3か所の539一致、独立した印刷操作行のHTML配信を確認した。

3.13.539 詳細画面からの印刷修正: 538の印刷ボタンは通常モードの詳細表示で非表示になるファイル操作行内にあり、詳細を表示したままクリックできなかった。印刷操作を独立行へ移し、詳細画面でも既存の失敗通知を表示するよう修正。JSONファイル操作の表示範囲・印刷処理・車両通信・保存形式は変更しない。ブラウザー検査を基本結果だけから、ライブ未取得・追加データの選択・絞り込み済みDTCへ拡張し、実ボタンのクリック、例外案内、印刷用表示と復帰、項目ごとの表示/非表示・元データ・検索語・選択項目の保持を確認。390幅のDTC印刷でP0420のみと1/2件の絞り込み表示を目視確認した。R1全工程と、隔離Chromeのオフライン一巡（ライブ推移の密データ検査を含む初回、絞り込み印刷を追加した通常再検査）に合格。ZIPは2,661,217 bytes、SHA256 2B0630406CE37A322D88A0D15C71D0FC95D164845900FF71B45CF20F49D0B885。別フォルダーへ全展開した配布コマンドで846対象ファイル/16,881,678 bytes一致。実プリンター・OS印刷ダイアログ・実VCI/実車は未検証。公開反映は別途確認する。

3.13.538 公開確認: 6c2b826cをGitHub mainへ送信後、公開サイト主要5資材のHTTP 200、script/service-worker/offline-assetsの3か所で538一致、indexの印刷ボタン配信を確認した。配布ZIPは2,661,195 bytes。印刷は現在の表示範囲の複製であり、全取得データの診断レポートや印刷完了の証明ではない。実機未確認の境界は維持する。

3.13.538 表示中の結果の印刷: 結果画面に明示操作の印刷ボタンを追加。現在の結果領域だけを印刷用に整え、別画面・入力欄・操作ボタンを除外する。値を表示する基本結果カードは残し、非表示項目や閉じた追加データを開かない。対象範囲とJSON保存とは別である旨を表示する。認証未解除・有効結果なし・取込/接続/読取処理中・結果画面非表示・二重開始を拒否し、印刷機能なし/例外時は元結果を保持して案内する。印刷後/取消/例外のスコープ解除を行い、保存済み扱いや診断判定を変更しない。単体53項目、R1一巡、配布396項目、隔離Chromeでの印刷イベント/印刷メディア表示/復帰/例外・JSON保存/再取込/模擬VCI/オフライン一巡に合格。印刷画面を目視確認した。実プリンター、OS印刷ダイアログ、実VCI/実車は未検証。既存JSON保存テストのDOM代替をセレクター別に修正し、印刷ボタンを通知欄と誤認しないようにした。ZIPを別フォルダーへ全展開し846対象ファイル/16,881,656 bytes一致を配布コマンドで確認。ZIP SHA256: D5AF48EC04B1002763DFE9429BE7A6E72E6C8B731CBE804B248EF82E6C974C88。公開反映は別途確認する。

3.13.537 公開・配布確認: 4f475334をGitHub mainへ送信し、公開サイト主要5資材のHTTP 200、版情報3か所の537一致と共通通信設定パネルの配信を確認。ZIPを別フォルダーへ展開して846対象ファイル/16,878,295 bytesの一致を配布検査コマンドで確認した。ZIP SHA256は0F8A675E1E499EF4ED1E8CCA6E011BB683B5625A1C9996C231C619DAA13B9C5A。配布検査396項目も合格。新規VCIの開発・受入条件をVCI-REQUIREMENTS.mdへ整理し、メーカーの公式資料に基づくUSB候補と実機未確認を分けた。機器購入・実車接続は実施していない。

2026-09-08 / 利用者方針: VCIは開発に合わせて新規購入予定。既存機器への適合情報待ちをソフト開発の停止理由にしない。購入機種はまだ固定せず、PCの読み取り専用経路を優先し、機器選定時は接続方式・必要コマンド・実機での受入条件を明示する。VCIの購入や実車接続は今回行わない。

3.13.537 通常画面の通信設定: 詳細画面にしかなかった既存の通信速度選択を、通常/詳細の両画面で使える共通の接続設定へ移動。選択肢・初期値・接続処理は変更せず、二重の設定値や自動速度探索を作らない。ELM327のPC Web Serial経路だけに表示し、認証未解除・機器選択・接続・初期化・読取・終了未確認・既存port・ブリッジ処理中は編集不可。通常画面の詳細ロックは速度設定に要求しないが、詳細画面では従来の詳細解除を要求する。68項目の状態検査をserial検査に追加。隔離ブラウザの模擬ポートに、通常画面で選んだ115200 bpsが実際に渡ること、選択/接続待ちの無効化、取消/失敗後の再編集と値保持、書込0回を確認。正常/異常読取・保存・再取込・オフライン復元を含む既存一巡も合格。390幅で共通設定と接続画面を目視確認。R1検査とPC起動1328項目が合格。実パスワード・実VCI・実車は使わず、車両通信権限・保存形式・診断判定は変更しない。

2026-09-08 / PC起動時の設定エラー案内: ポート範囲不正、短い接続キー、再生ログ環境変数の残存を個別の日本語メッセージに分け、対応する設定名・復帰方法・未起動を表示する。設定値やログパス・接続キーは表示せず、ポート変更時の保存領域の違いと元データ保持、ログ削除不要を明記。既存の入力制限と起動拒否は変更しない。実子プロセスで4つの設定異常を起動し、終了コード1・原因別案内・標準出力なし・秘密値非表示を確認。Windows起動検査の従来の汎用文言照合を原因別案内へ更新し、PC起動1328項目/Errors 0、配布396項目/Errors 0に合格。Webアプリ・版番号・保存形式・実車通信は変更せず、既存の配布ZIPにもまだ反映していない。

2026-09-08 / 再起動後の再保存障害をアプリ外へ切り分け: `validate-scanner-file-flow.cjs --restart --live`でキャッシュ再起動と記録再取込後、ライブ結果の再保存時にブラウザープロセスが終了することを再現。独立した`node scripts/inspect-browser-restart-download.cjs`（PLAYWRIGHT_MODULE指定が必要な環境あり）を追加し、アプリ・Service Worker・車両I/Oを使わず、模擬JSONのBlobダウンロードだけでも一時プロファイルのブラウザー再起動後に同じsaveAs失敗を確認した。Chrome 152.0.7977.77とEdge 152.0.4191.66で再現し、Chromeの再起動なし対照（`--no-restart`）では2回とも保存内容一致で合格。DEBUG=pw:browserのChromeログでアプリ試験のexitCode=3758096392、最小再現のexitCode=3221225477を記録。タスクバー初期化エラーも出るが、それが終了原因とは断定しない。最小画面を目視確認し、ページ例外・予期しない外部要求は0。各実行の一時フォルダーにreport.json・画面・成功した模擬JSONを残し、失敗は終了コード1で扱う。ブラウザー/Windows/自動操作のどの内部要因かは未確定であり、通常の手動保存まで失敗すると一般化しない。既存利用者プロファイル・ブラウザー設定・アプリ・配布ZIPは変更せず、総合合格へ算入しない。

2026-09-08 / 配布案内の自動同梱: 別添ガイドだけに依存しないよう、今後の配布生成時のREADMEへ整備事例と読取結果の別々のバックアップ/復元、旧版を残す別フォルダー展開、同一ブラウザープロファイルとURLの保存先境界、更新失敗時に保存領域を消さない復帰手順を追加。対象版は生成元の版から埋め込み、任意旧版の保存形式互換性や実車適合を保証しない。配布検査は説明の必須事項とREADMEの内容破損・サイズ変更・欠落・復元を追加して396項目/Errors 0。既存3.13.536 ZIPは再生成せずSHA256一致を確認し、公開アプリ・版番号・保存形式・車両通信は変更しない。新しい案内は今後生成する配布物が対象であり、既存ZIPへの反映済みとはしない。

2026-09-08 / 3.13.536 更新失敗からの復帰検査: 配布版更新検査へ `--failed-update` を追加。3.13.530から3.13.536へ移る試験専用サーバーでstyle.cssにHTTP 503を返し、workerの更新資材取得に実際に到達したことを要求件数で確認。失敗通知、不完全な新版キャッシュの破棄、旧版worker/全資材キャッシュ・模擬事例1件の保存文字列/件数・未解除状態の維持を検査した。サーバー停止・通信遮断後も旧版をService Worker応答から再読込でき、健全な試験サーバーを明示的に起動し直すと新版への更新、続く旧版復帰も合格。失敗表示は診断補助タブで390幅を目視確認。通常モードも再検証して合格。障害はメモリ内の試験サーバーだけに注入し、両配布物は試験前後で整合検査一致。アプリ本体・版番号・配布ZIP・実データ・車両通信は変更せず、自動再試行も追加しない。取得失敗1資材の同一PC試験であり、電源断・ディスク満杯・別PC・実車まで確認した意味ではない。

2026-09-08 / 3.13.536 配布版の更新・旧版復帰: `node scripts/validate-packaged-update.cjs <旧版フォルダー> <新版フォルダー>`（Playwright必要）を追加。整合検査済みの3.13.530→3.13.536→3.13.530を同一PC・同一originで実サーバー切替し、隔離Chromeで模擬事例1件のJSON取込後の保存文字列・件数・未解除状態が全段階で不変と確認。各段階で全資材キャッシュとcontrollerの版を照合し、両サーバー停止・ネットワーク無効後のService Worker応答と表示版を検査。新版検出通知は更新時のみ、旧版への復帰時は既存の画面/基盤の版不一致案内を確認して再読込する。旧版も「更新版」通知を出すという最初の試験前提を修正した。新版/復帰後の390幅画面を目視確認。アプリ例外と予期しない外部要求は0。未同梱favicon.icoへの自動要求によるERR_FAILED/404だけは別報告。両パッケージは試験前後で整合検査一致。実データ・実パスワード・実VCI・別PC・OS再起動・保存形式移行は未検証。アプリ本体・版番号・配布ZIPは変更しない。

2026-09-08 / 3.13.536 総合検証追補: validate:releaseでECU情報表示の古い文字列照合（items.map）が失敗したため、formatterの実行結果を検査する1項目へ置換し、公開検査数7447との整合も維持した。既存のECU表示検査をOBD検査から実行し、camel/snakeのDID表記・記録値の無解釈表示・入力不変性を追加（87項目）。修正後のvalidate:releaseは全工程合格: OBD主集計7447、bridge384、J2534 lifecycle242/supervisor154、PC起動1313、配布389、offline180、データJSON203ファイル/DTC3878件、Errors 0/Warnings 0。validate:r1とキャッシュ待機回帰16項目も合格。配布ZIPのSHA256は9A60F8865BE622EF13BD7D3E5DD2FE9B6372F164BF09DD7D2AAB81A9410C4C04で変更なし。検査コードの修正だけで、アプリ本体・版番号・車両送信・保存形式は変更していない。ソフト検査の合格を実VCI・別PC・実車適合の証明にはしない。

2026-09-08 / 3.13.536 待機処理の横断修正: scripts内で残っていた非同期waitForFunctionは診断結果ファイル一巡検査のキャッシュ準備1か所。配布版検査と同じく、Node側で解決値を待つ60秒上限のポーリングへ修正した。`node scripts/validate-browser-cache-wait.cjs` は両検査の実際の待機ループを抽出し、false継続・即時成功・時間切れ・例外伝播をブラウザなしで検査（16項目合格）。`node scripts/validate-scanner-file-flow.cjs --offline --dense-live`（Playwright必要）も合格し、模擬認証、JSON取込/保存/再取込、60点ライブ推移と複数ECU、詳細遷移、検索保持、読込異常・遅着、接続取消、無応答/正常0件、通信障害後と無応答後の復帰、オフライン保存復元を確認。390幅の追加データ画面も目視確認。車両通信・実パスワード・既存利用者データ・アプリ本体・版番号・配布ZIPは変更しない。

2026-09-08 / 3.13.536 配布版再起動の検証修正: `validate-packaged-browser.cjs <展開した配布フォルダー> --restart` を追加。初期試験では再起動時にERR_INTERNET_DISCONNECTED（遮断模擬なしではERR_CONNECTION_REFUSED）となったが、保存状態の比較でサーバー停止前からキャッシュ・active workerが未準備と判明。使用中のPlaywright 1.62.1実装はwaitForFunctionの非同期predicateが返すPromiseをtruthyとして扱い、解決値falseを再ポーリングしない。試験をNode側でpage.evaluateの解決値を待つ60秒上限のポーリングへ変更した。変更後、キャッシュ18,352,896 bytesとService Worker 8,893 bytesがブラウザ終了前後で保持され、両サーバー停止・通信遮断・同じ隔離プロファイルでのChrome再起動後にService Worker応答で画面が開くことを確認。初回注意の承諾保持、ロック保持、誤入力消去、事例未保存、390/1280幅も検査し390幅を目視確認。通常再読込モードも再検証して合格。再起動失敗はこの試験の待機不足であり、アプリ本体の不具合という前段の疑いを訂正する。起動前のabout:blankではCDPのキャッシュ名取得がNo frame foundとなるが、容量取得と実ナビゲーションの応答で別途確認する。実パスワード解除・OS再起動・別PC・実車は未検証。アプリ・版番号・ZIP・実データ・車両通信は変更していない。

2026-09-08 / 3.13.536 配布検証追補: 配布ZIPを別の一時フォルダーへ展開し、846ファイルの整合性を確認。専用検査 `node scripts/validate-packaged-browser.cjs <展開した配布フォルダー>`（Playwright必要）を追加した。配布物内の実サーバーを空のドライバー登録fixtureで起動し、隔離Chromeで初回注意の確認、誤パスワード拒否、全Web資材のキャッシュ、両サーバー停止・ネットワーク無効化後の再読込とロック保持を確認。390/1280幅を目視確認し、配布物の変更・整備事例保存・車両通信はない。未同梱favicon.icoへのブラウザ自動要求だけがオフライン時に失敗するため、専用検査はそのURLとエラーの完全一致だけを別報告し、アプリのエラーは引き続き失敗扱いとする。実パスワードによる解除、ブラウザ/OS再起動、別PC、実VCIは今回未検証。配布検査389項目は前工程で合格済み。アプリ本体・版番号・配布ZIPは変更しない。

2026-09-08 / 3.13.536: 整備事例JSON取込の結果が登録タブにしか表示されない不具合を実ブラウザで再現。データ管理にも読込中・成功・解析失敗・読込失敗・保存失敗・中断を表示する。既存の登録側表示も保持し、role=statusで通知する。隔離Chromeで不正ファイル、正常再試行、バックアップ、再読込、重複取込、遅延した失敗・再選択・全削除を検証する専用スクリプトを追加（node scripts/validate-case-import-browser.cjs、Playwright必要）。実データ・保存形式・診断判定・車両通信は変更しない。

2026-09-07 / 3.13.535: 整備事例の全削除・保存内容の再読込が成功した際、進行中のJSONインポートを失効させる。遅れて届くload/error/abortで削除済みデータが復活したり結果表示が上書きされたりしないよう修正。削除確認の取消・削除失敗・再読込失敗では既存データと進行中の読込を維持する。実データの削除は行わず、隔離した模擬ストレージで検査。保存形式・診断判定・車両通信は変更しない。

2026-09-07 / 3.13.534: 整備事例JSONの再選択時は前の読込を失効させ、最新の選択だけを採用する。古いload/error/abortが後着しても保存内容・結果表示・新しい選択を変更しない。中断API不在・同期中断通知・中断例外でも所有確認で保護し、新しい読込が失敗しても古い結果へ戻さない。保存形式・診断判定・車両通信は変更しない。

2026-09-07 / 3.13.533: 整備事例JSONインポートのファイル読込失敗・中断・同期例外を捕捉し、既存の成功表示を失敗案内に更新してファイル選択を解除する。保存済み事例や表示済み一覧を変更せず、同じファイルの再選択と正常な再試行を可能にする。FileReader生成・読込開始・error・abortの4経路を模擬検査。事例保存形式・診断判定・車両通信は変更しない。

2026-09-07 / 3.13.532: レディネスの要約と詳細の不明状態を一致させる。真偽値以外の対応・完了フラグを要約から除外せず、詳細と同じく状態不明として数える。空の主配列より内容のあるECU別名配列を優先し、未取得項目だけの記録も詳細から落とさない。保存フラグの変換・診断判定・車両通信は変更しない。

2026-09-07 / 3.13.531: Mode06のECU別記録がある場合も、全体の記録済み読取状態を独立表示する。ECU別の取得成功によって全体の読取拒否・応答未解析・状態未確認が表示から落ちる不具合を修正。全体状態をECUへ付け替えず、未記録の全体状態は作らない。表示のみの変更で、診断判定・保存形式・車両通信は変更しない。

2026-09-07 / 3.13.530 検証追補: 配布物の検査ファイル同時欠落を、cmd・npm起動・単独検査・J2534証拠確認を含む10経路で確認。起動・検査処理・接続キー出力へ進まず、欠落ファイルも自動修復しない。元のファイル復元後は3起動経路が再度整合検査を通って起動することを確認した。Windows上の配布検査389項目が通過。本体・保存形式・車両通信・配布ZIPは変更しない。

2026-09-07 / 3.13.530: 配布版の起動ファイルを検査必須のテンプレートへ分離。package-info.jsonとpackage-integrity.jsonの両方を失うと開発用経路に入り検査を飛ばす不具合を修正した。配布作成時だけstart-packaged-workstation.cmdをstart-workstation.cmdとして同梱する。元の開発用起動、利用者の起動ファイル名、診断保存形式・車両通信は変更しない。実パッケージの両ファイル欠落で、起動・ペアリング値出力に到達しないことを回帰検査する。

2026-09-07 / 3.13.529: オフライン配布物の照合一覧に必要な起動部品・Web資材が含まれることを検査する。従来は一覧の一部を欠いても件数等が一致すれば検査に通るケースがあった。offline-assetsの型・件数・パス・重複・必須資材も検査し、自動修復はしない。配布物の形式・診断記録・車両通信は変更しない。照合はコピー整合性の検査であり、署名や発行元真正性の証明にはならない。

2026-09-07 / 3.13.528: レディネスの読取状態・エラー理由を監視項目の絞り込みから独立した欄へ表示。全体とECU別を区別し、明細がなくても記録済み状態・失敗理由を確認できる。検索項目がない場合は操作欄を非表示にする。表示検査で失敗理由の欠落を再現後、隔離ブラウザで失敗記録のJSON再読込、詳細表示、絞り込み中の理由保持、390/1280幅の明暗表示、保存データ不変性を確認。診断判定・保存形式・車両通信は変更しない。

2026-09-07 / 3.13.527: ECU情報・対応PID・レディネス詳細の配列参照を修正。空のcamelCase配列で内容のあるsnake_case記録が隠れるケースを防ぎ、対応PIDのページ・全体／ECU別エラーにも同じ扱いを適用する。両方に記録がある場合は従来の優先順位を維持し、混合・集約値の再構成・失敗記録の対応済み扱いはしない。専用表示検査と隔離ブラウザの既存操作回帰を確認。保存形式・正規化・診断判定・車両通信は変更しない。

2026-09-07 / 3.13.526: ECU情報でエラー理由・負応答のみの記録が詳細から落ちる不具合を修正。ECU別明細がある場合も、全体の読取状態・負応答・エラーを別表示し、特定ECUの成功や失敗へ付け替えない。空のerrorCodesより内容のあるerror_codesを優先する。表示明細の検索対象へ理由を混ぜず、保存形式・診断判定・車両通信は変更しない。専用表示検査で不具合を再現後、理由の保持と元データ不変性を確認。

2026-09-07 / 3.13.525: ECU情報と対応PIDの詳細へECU・項目の複数語検索を追加。検索対象は識別情報を非表示にした表示明細だけとし、読取状態・エラー・取得状況は検索中も維持する。該当なしを未対応・正常と扱わず、解除で全明細へ戻す。保存形式・診断判定・車両通信は変更しない。専用表示検査と隔離した実ブラウザで、条件の組合せ、VIN非表示、状態表示維持、390/1280幅の明暗表示、保存データ不変性を確認。

2026-09-07 / 3.13.524: フリーズフレームとMode06に明細がない場合も、記録された読取状態があれば詳細カードを表示。読取拒否・応答未解析・状態未確認と、全体／ECU別の記録済みエラー理由を確認できるようにした。FFの「起点情報のみ取得」は起点情報が存在する場合に限定し、値なしを正常と扱わない。空のcamelCase配列より内容のあるsnake_case配列を優先する表示経路も検査。保存形式・判定ロジック・再送・車両通信は変更しない。`validate-mode06-display.js` と実ブラウザ検査で、失敗した読取記録のJSON読込、詳細遷移、理由表示、元データ不変性を確認する。

2026-09-07 / 3.13.523: Mode06詳細へECU・TID・CIDの複数語検索と、記録判定（不合格／未判定／合格）の絞り込みを追加。元の集計・ECU読取状態・整備書確認の注意は維持し、明細だけを絞り込む。該当なしを正常判定にせず、明細なしの集計記録は明細未取得と表示する。判定の再計算・保存形式・車両通信は変更しない。専用UI検査と実ブラウザ検査にMode06検索、未知の状態、該当なし、警告保持、解除、保存データ不変性を追加。

2026-09-07 / 3.13.522: フリーズフレームの詳細に項目検索・ECU・FF番号・未換算のみの絞り込みを追加。選択条件は記録値の表示だけに適用し、起点DTC・取得状況・ECU別の記録情報は維持する。FF #0と番号未記録を区別し、数値0も欠落させない。現在のライブ値とは区別して案内し、保存形式・診断判定・車両通信を変更しない。`validate:readiness-review` と `validate:readiness-review-browser` はフリーズフレームの組合せ条件・解除・元データ不変性も検査する。

2026-09-07 / 3.13.521: レディネスの未完了・不明・未取得を個別に絞り込み可能にし、ECU別グループ内の状態内訳を表示。項目の「参考情報・整備書確認」から同梱カタログの用途・未完了時の注意・整備書確認要否・出典を表示する。参考情報は同梱カタログのID一致に限定し、読込ファイルから出典や推奨作業を追加しない。一般参考情報と対象車両の適合・故障確定を区別し、未完了向け注意は未完了項目だけに表示。保存形式・診断判定・車両通信は変更しない。

2026-09-07 / 3.13.520: 通常の読取結果画面にECU別レディネス確認を実装。基本結果の「レディネス」から詳細を開き、監視項目名・ECUの検索と、未完了・不明・未取得／完了／非対応の絞り込みを使用できる。既存の正規化済み読取結果のみを表示し、ECU間の合算判定、故障解消・車検適合判定、車両への送信は追加しない。JSONファイル読込から検索・解除・結果への往復、390/1280幅の明暗表示、操作前後の保存データ不変性と外部要求なしを実ブラウザで確認。専用検査は `npm run validate:readiness-review` と `npm run validate:readiness-review-browser`（Playwrightが必要）。以下は履歴。

2026-09-07 / 3.13.519後続: readiness派生値の抽出段階で同一0101を繰り返し解析していた処理を共通化。sequence検査は維持し、派生3種の取得結果・寿命を検査。関連8647件がErrors 0。通常API・保存・実車送信は変更なし。詳細は `R2-MINIMAL-COMPARISON-EVIDENCE-DESIGN.md` 冒頭。以下は履歴。

2026-09-07 / 3.13.519後続: Node専用pair handleへ非連続monitorの模擬状態取得を追加。spark/compressionをECU/前後別に扱い、未定義bitや矛盾を保留。関連8635件がErrors 0。readiness全体の集計・実車適合・成功判定・通常API・保存・送信は未追加。詳細は `R2-MINIMAL-COMPARISON-EVIDENCE-DESIGN.md` 冒頭。以下は履歴。

2026-09-07 / 3.13.519後続: Node専用pair handleへ基本3monitorの模擬状態取得を追加。未対応を完了とせず、矛盾・予約bitをindeterminateとする。B全256値など関連4018件がErrors 0。非連続monitor・readiness全体・実車の成功判定・保存・送信は未追加。詳細と根拠は `R2-MINIMAL-COMPARISON-EVIDENCE-DESIGN.md` 冒頭。以下は履歴。

2026-09-07 / 3.13.519後続: Node専用pair handleにMIL指示・報告DTC件数の取得を追加。0101の検証済みコピーからECU別に抽出し、全256値など関連3242件がErrors 0。monitor完了・DTC一覧との件数照合・実車比較・成功判定・保存・送信は未追加。詳細と符号化根拠は `R2-MINIMAL-COMPARISON-EVIDENCE-DESIGN.md` 冒頭。以下は履歴。

3.13.519後続の模擬clear入力上限: 不変性検査を非再帰化し、own key総数の上限と上限超過専用エラーを追加。深さ5000・幅5000・循環・正当な32-source・opaque tokenの検証を追加し、関連2464件がErrors 0。アプリ本体・保存・実車送信は変更なし。詳細は `R2-MINIMAL-COMPARISON-EVIDENCE-DESIGN.md` 冒頭。以下は履歴。

3.13.519後続の模擬差分境界検証: 全256集合ペア、255件、32 ECU、順序反転、期待ECU欠落、受信上限超過の回帰検査1084件を追加。関連2459件がErrors 0。差分処理本体・アプリ・保存・実車送信は変更なし。詳細と単独検証コマンドは `R2-MINIMAL-COMPARISON-EVIDENCE-DESIGN.md` 冒頭。以下は履歴。

3.13.519後続の模擬DTC差分: Node専用pair handleにinspectDifferenceを追加し、内部の組証拠だけからintent/source別added/removed/retainedを取得する。scope/context不一致・失効・dispose後は取得拒否。関連1375件がErrors 0。実車の比較可能性・消去成功・故障解消は推定せず、保存・通常API・送信は変更なし。詳細は `R2-MINIMAL-COMPARISON-EVIDENCE-DESIGN.md` 冒頭。以下は履歴。

3.13.519後続の前後証拠一括生成: Node専用factoryで固定コピーからsequence検査とDTC抽出を実行し、両側成立時のみ一つのhandleを生成。模擬clear snapshotの内部不変性、scope寿命、一括disposeを検査。関連1260件がErrors 0。差分・保存・実車送信は追加しない。次は内部の組証拠だけを扱う模擬DTC差分の設計/テスト。詳細は `R2-MINIMAL-COMPARISON-EVIDENCE-DESIGN.md` 冒頭。以下は履歴。

3.13.519後続の前後証拠結び付け: 単独handleの成立とsequence成立を区別する38件の回帰検査を追加し、関連1103件がErrors 0。summary同値・別scope・attempt使い回し・時系列の重なり・失効を検査。一括生成factoryで固定コピーからsequence検査/抽出を行う方針を `R2-MINIMAL-COMPARISON-EVIDENCE-DESIGN.md` 冒頭へ記載。factory本体・差分・保存・送信は未追加。次はこのNode専用factoryを実装する。以下は履歴。

3.13.519後続のpost DTC証拠抽出: beforeと入力コピー・抽出・寿命管理を共通化し、模擬clear終端とscoped-post評価の成立時だけテスト用証拠を返す。関連1065件がErrors 0。アプリ本体・保存・比較・送信は変更なし。次は前後証拠の組合せに必要なscope/attempt/順序の結び付け設計。詳細は `R2-MINIMAL-COMPARISON-EVIDENCE-DESIGN.md` 冒頭。以下は履歴。

3.13.519後続のbefore DTC証拠抽出: 検証済みコピーからintent/source別コード集合を作るテスト用handleを追加。失効・context不一致・dispose後は取得を拒否し、raw応答やtokenを出力しない。関連1015件がErrors 0。通常API・保存・比較・送信は変更しない。次はpost側の証拠抽出。詳細は `R2-MINIMAL-COMPARISON-EVIDENCE-DESIGN.md` 冒頭。

3.13.519後続の最小比較証拠設計: `R2-MINIMAL-COMPARISON-EVIDENCE-DESIGN.md` にintent/source別のDTC集合、検証済み内部コピー、短命な保持、失効と非永続化の要件を整理。DTC消失・追加・置換・status移動やreadiness変化をsequence成立と区別する回帰試験を追加した。比較用summaryや差分、通常API、保存形式、実送信は追加していない。次はテスト内のDTC最小証拠抽出を具体化する。

3.13.519後続の模擬順序検査: before/clear/postを一つのfixture scopeで評価し、3 attemptの相違と取得区間の順序を検査するNode用処理を追加。関連910件がErrors 0。caller入力の時刻・参照を実clear接続の証明にせず、成功推定・比較・送信flagはfalse。アプリ本体・版番号・保存形式は変更しない。次はsource別の最小証拠summaryの設計。詳細は `R2-BEFORE-READOUT-SCOPE-DESIGN.md` 冒頭。

3.13.519後続の消去後scope照合: Node専用ハーネスにpost側を追加し、before側とsource照合を共有した。clear未完了・completion矛盾・attempt再利用・順序逆転をブロックし、Mode04の期待sourceを読取対象へ転用しない。関連782件がErrors 0。アプリ本体と版番号は変更しない。次はbefore/clear/postを同じ模擬範囲と接続世代へ束縛する境界設計。前後比較や実車送信は未実装のまま。

3.13.519後続の模擬scope照合: テスト専用handleとstrictなbefore receiptを照合するNode用ハーネスを追加した。肯定応答の不足・範囲外・矛盾・途中終了を一致扱いにせず、評価前後の失効照会と接続参照一致を確認する。照合126件・scope48件・before205件・post239件がErrors 0。実車網羅性や成功推定のflagはfalse。アプリ本体と版番号は変更せず、次はpost側の模擬照合と各取得境界の設計へ進む。詳細は `R2-BEFORE-READOUT-SCOPE-DESIGN.md` 冒頭。

3.13.519後続のfixture scope: テスト専用モデルでintent別のECU範囲を独立入力し、空・重複・不正IDを拒否、接続/対象参照の不一致と一方向の失効を検査する。scope48件・before205件・post239件がErrors 0。アプリ本体・通常API・保存形式・版番号は変更しない。詳細は `R2-BEFORE-READOUT-SCOPE-DESIGN.md` 冒頭。次は模擬receiptとの範囲照合設計であり、実車の期待ECUや前後比較の実装ではない。

3.13.519 消去前の模擬読取評価: before専用APIを追加し、strict receipt検査とsource観測をpostと共有した。既存post契約を維持し、消去開始や期待ECU範囲を推定しない。before205件・post239件の試験が通過。通常UI・保存・workflow・実送信への接続なし。詳細は `R2-BEFORE-READOUT-SCOPE-DESIGN.md`。次は独立したfixture scopeの最小契約を設計し、前後比較はまだ実装しない。以下の過去版説明よりこの節を優先する。

3.13.519の最終検証: `validate:obd` は終了コード0、主集計7447件・Errors 0。before205件・post239件と各補助検証も同実行で通過。bridge384件・offline180件、構文・差分検査、localhost:3001のHTTP 200とbefore API配信確認も通過。途中でテスト本体の意図しない欠落を発見したため、HEADの内容と追加import1行へ機械的に復元し、差分を照合して全体を再実行した。復元前の失敗runは合格として集計しない。UIブラウザ・実車試験・追加エージェントレビューは実施していない。

3.13.518後続の設計・テスト更新: `R2-BEFORE-READOUT-SCOPE-DESIGN.md` にbefore証拠・intent別範囲・接続失効・比較保留の設計案を整理した。全intentの肯定応答やMode04側のECU範囲から網羅性を作らず、caller指定のbefore/expected-source/verified項目を拒否する回帰試験を追加。アプリ本体・API・保存形式・版番号は変更しない。次の単位はbefore専用の最小入出力設計と共通検査の切り出しであり、前後比較や実送信ではない。

3.13.518 保留・永久DTCの限定意味検証: Scapy公式実装を照合し、03の件数検証を07/0Aにも適用した。source単位の空/非空観測だけを追加し、模擬評価・非送信・比較不可の境界は維持する。receipt215件と関連parser・応答評価・受信管理326件（合計541件）、offline180件がErrors 0。構文・差分検査、localhost:3001のHTTP 200と追加処理の配信確認も合格。OBD全体・bridge・UIブラウザ・実車試験は今回再実行していない。before証拠・intent別の期待source・前後比較は未完了。詳細と根拠は `R2-POST-READOUT-COMPARISON-DESIGN.md` の同版節。以下の過去版説明よりこの現在地を優先する。

3.13.517 Mode 03の限定意味検証: CANの件数byteと後続DTC対の長さをECU別に照合する。完結した矛盾のないreceiptだけでsource単位の空・非空を観測し、件数不一致・重複矛盾・途中終了から結論を出さない。07/0Aの意味検証、before証拠、期待source集合、前後比較、実車通信への統合は引き続き未完了。新しい保存形式・通常診断画面の変更なし。根拠・受入制限は `R2-POST-READOUT-COMPARISON-DESIGN.md` の同版節。

3.13.517の限定検証: receipt99件、read-only parser53件、Mode04 parser56件、消去応答評価54件・受信管理163件（合計425件）、offline180件が終了コード0・Errors 0。構文・差分検査、localhost:3001のHTTP 200と追加処理の配信確認も合格。Mode 03の0/1/2/3/127/255件、application payload切断・余剰、同sourceの矛盾・重複、複数ECU、negative・wrong service・NO DATA混在を検査した。今回の変更範囲に検証を限定し、OBD全体7,447件・bridge・UIブラウザ・実車試験は再実行していない。

3.13.516 R2再読取の模擬評価: 固定CAN表示形式の03/07/0A/0101応答を厳密に組み立てるparserと、4つのreceiptの順序・attempt・観測sourceを扱う純粋評価器を追加した。複数ECUの応答混在、連番不正、途中終了、NO DATAと応答の矛盾を区別する。DTCの件数byteを既存一般decoderで解釈せず、意味未検証として空・非空の断定を保留する。Mode04の期待sourceを再読取の期待ECUへ転用せず、接続・車両・消去範囲の検証済み表示も作らない。

この版で通常画面の操作や実車送信は増えない。内部の応答組立から4 receiptの評価までを模擬データで接続する段階であり、DTC意味検証、前後比較、監査保存、実dispatcherとの接続は未完了。通常の読取、診断判定、保存形式は変更しない。設計と参照資料は `R2-POST-READOUT-COMPARISON-DESIGN.md` の同版節。

3.13.516の検証: Sol設計・最終レビューで重大な指摘なし。最終コードの `validate:obd` は終了コード0、主集計7447件・Errors 0。新規parser53件・receipt46件、既存Mode04 56件、応答評価54件・受信管理163件も同じ実行で通過した。bridge384件、offline180件、構文・差分検査も通過。ローカルHTTP 200で両APIの配信を確認した。別タスクで中断された途中runは完了件数に含めない。UI変更なしのためブラウザ一巡の再実行なし。実記録・実車への操作なし。

### R2の実装順序と残り工程（2026-09-06）

公開アプリ3.13.518はHTTP確認済み。以下は消去後の比較に進むためのコード照合結果と実装順序である。

| 工程 | 現在の状態 | 次に満たす条件 |
| --- | --- | --- |
| 消去前記録の保存・照合・保護 | 実装・模擬検証済み | 実車同一性やECU作用範囲の確認とは分離して維持 |
| 消去応答の解析・受信管理 | 非送信モデルを実装・模擬検証済み | 実際の要求・対象接続と結び付けるdispatcherは別工程 |
| 応答後の再確認計画 | 4つのread-only intentを実装済み | 計画の存在を再読取の実施済み証拠にしない |
| 再読取の応答根拠 | 4 receiptの模擬評価を実装、03/07/0Aの限定件数検査を追加 | intent別の期待sourceとbefore証拠を整備する |
| DTC・レディネスの前後比較 | 設計対象・未実装 | 同一の対象・取得範囲で比較し、空配列だけでは消去確認にしない |
| 実行・再読取・監査の一巡 | 未実装 | 通信所有権、対象適合、確認失効、結果不明時の復旧と記録を統合 |
| 対象実車での試用 | 未実施 | 上記を模擬検証後、対象車両と危険を説明して利用者が実行判断 |

3.13.516までのSol設計・レビューと、その後の03/07/0A限定検査は `R2-POST-READOUT-COMPARISON-DESIGN.md` に記載する。次はbefore証拠とintent別の期待source集合の設計であり、前後比較APIはまだ固定しない。前側にも同等の根拠を用意して模擬比較を実装し、その後に実行経路へ接続する。既存のWeb Serial取込では一部の `NO DATA` が空のDTC一覧へ正規化されるため、既存セッションの空配列や `reported` だけを、ECUがDTCなしと肯定応答した証拠へ転用しない。既存の取込互換性はこの工程で変更しない。

今回の再確認は応答評価54件・受信管理163件・ELM応答解析56件がエラー0、ローカルHTTP 200。既存273件の再実行であり、新しい実装件数・車種実績ではない。

3.13.515の全体回帰: 最終コードで `validate:obd` が終了コード0、主集計7447件・Errors 0で完了。実行に含まれるJSON出力250件、シリアル統合332件・ライフサイクル1021件、記録127件も合格した。テスト件数は車種対応数や実車確認数ではない。

3.13.515の専用検証: Sol設計・最終レビュー済み。応答評価54件、受信管理163件、ELM応答解析56件、bridge384件、offline180件がエラー0。肯定・混在・無応答・途中終了と既存の解析経路で、同じ凍結計画と非送信フラグが保持されることを確認した。実記録・実車試験は行っていない。UI変更がないためブラウザ一巡は今回再実行していない。

3.13.515 R2応答後の再読取要件: 既存の消去応答評価と受信結果に、保存・保留・永久DTCとレディネスの再読取計画を接続する。正常な肯定観測でも再確認を省略せず、一部無応答や結果不明でも自動再送しない。実行結果や修理完了は推定しない。これは非送信の模擬経路で応答評価から再確認要件までをつなぐ工程であり、再読取の実行、前後比較、監査保存、実消去の開放は未完了。設計は `R2-DTC-CLEAR-DESIGN.md` の同版節。既存の診断判定・保存形式・許可リスト・画面操作は変更しない。

3.13.514の検証: Sol設計・最終レビュー済み。閲覧ブラウザで通常・欠損・矛盾した接続根拠、古い関連付けの失効、確認条件と送信フラグの不変、320/390/1280pxの表示を確認した。保存ブラウザ、記録127件、対象参照39件、JSON出力250件、詳細遷移59件、offline180件が合格。表示のみの変更のためOBD全体主集計は再実行していない。実記録・実車操作なし。

3.13.514 R2準備表示の区別: 照合済み記録の欄で、端末内の読取内容の一致・比較保護と、接続参照・車両同一性・ECU消去範囲を分けて表示する。既存の関連付け、保護、対象確認スナップショットだけを参照し、未確認条件を自動達成しない。従来の条件一覧と比較解除を維持する。Solによる表示契約の設計に基づくUI整理であり、実車対応率・消去機能の開放・新たな証拠取得を意味しない。診断判定・保存形式・車両送信は変更しない。

再ダウンロードの追加切り分け: Chrome 152.0.7977.65で通常の自動保存と、`--native-download --online` によるCDP `allow` 保存の両方が初回成功・再起動後失敗となった。後者は `saveAs` を使わず、実ファイルの完全一致とブラウザ接続状態を検査するため、`saveAs` 単独原因という仮説では説明できない。既存ファイルを成功と誤認しない事前検査と20秒上限を追加した。これは修復ではなく原因切り分け用の試験追加。保存制御の仕様: [Chrome DevTools Protocol](https://chromedevtools.github.io/devtools-protocol/tot/Browser/#method-setDownloadBehavior)。

2026-09-06 / 3.13.513 R1再起動の再検証: 一時Chromeプロファイルを終了・再起動し、配信サーバー停止とネットワーク無効化の両方を維持してキャッシュ起動、保存JSONの復元、詳細遷移、検索保持、不正ファイル拒否、接続取消・失敗・再試行まで合格した。実ユーザープロファイル・実車・OS再起動は対象外。再起動後の再ダウンロードは別問題として残す。診断ツールを読み込まない最小ページでもChrome/Edgeで初回保存後の再起動・再保存が失敗し、Chromeプロセス終了コード3221225477を確認した。オンライン比較でも失敗、初回保存を省いた比較では成功したため、診断ロジック由来とは断定しない。ブラウザ自動操作環境での再現であり、通常の手動操作でも起きることやブラウザ内部の原因を証明したものではない。再現試験: `scripts/reproduce-browser-download-restart.cjs`。製品コード・版番号・保存形式・車両送信は変更していない。

3.13.513 R2記録の比較保護: 比較に使う記録を同一保存領域の対応タブ間で予約し、予約中の削除を止める。削除側も同じ記録単位のWeb Lockを取得してから既存の内容照合と削除後確認を行う。比較解除・画面終了時に解放し、競合時に自動再試行しない。Web Locks非対応時は比較と削除を開放せず、保存・一覧・読込・出力を維持する。協調する新版同士の記録保護であり、旧版・直接DB操作・サイトデータ消去や車両通信の排他は対象外。保存形式・診断判定・車両送信は変更しない。設計は `R2-JOURNAL-RESERVATION-DESIGN.md`。

検証: Sol設計・最終レビュー完了。OBD全体主集計7447件エラー0、接続対象39件、シリアル統合332件・ライフサイクル1021件、session export250件、詳細遷移59件、bridge384件、offline180件、保存ブラウザが合格。全体試験後のレビュー修正は記録専用127件、実ブラウザ29件、閲覧ブラウザの比較・解除・削除・画面幅検証で再確認した。全体試験には修正前の記録専用122件が含まれるため、最終127件とは分けて記録する。実記録・実車操作は行っていない。今回の保護は記録単位であり、車両通信の別タブ排他や消去送信の完成を意味しない。

3.13.512 R2接続と記録の整合確認: 明示照合した記録候補に対し、現在の読取結果と接続世代の整合を内部で検査する工程。車種名・記録IDの一致から実車同一性や消去範囲を推定せず、車両識別と作用範囲の不足を残す。対象設定・準備条件の自動達成・消去送信・保存形式変更は行わない。利用者向けの追加操作は設けず、既存の照合操作と失効条件へ接続する。

検証: Sol設計・再レビュー完了。OBD全体主集計7447件エラー0、シリアル統合332件・ライフサイクル1021件、session export250件、画面遷移・詳細59件、bridge384件、offline180件、保存ブラウザが合格。全体試験開始後のレビュー修正については専用39件と閲覧ブラウザを再実行し、未接続時の同一オブジェクト変更も再描画前に失効することを確認した。全体試験は修正前の専用32件を含むため、最終39件の結果とは分けて記録する。実データ操作・実車試験は未実施。実車識別、ECU作用範囲、監査、別タブ排他、消去送信は未完了のまま。

3.13.511 R1/R2操作整理: 保存記録の操作を「保管」「一覧」「選択した記録」に分ける。ボタンの権限・無効条件・処理は変更せず、保存・閲覧・照合・出力を操作対象別に配置する。診断判定、保存形式、車両通信の変更はない。R2の次工程は、ローカル記録との一致を実車同一性と混同しない対象結び付けの設計とする。UI整理を実車対応やDTC消去開放の進捗へ換算しない。

検証: 閲覧・保存の隔離ブラウザ試験、画面遷移と詳細59件、offline180件、構文・差分検査が合格。1280px/390pxの実表示と全操作ボタンの中心点hit-testで固定メニューによる遮蔽がないことを確認。実データ操作・実車試験は行わず、診断ロジックに変更がないためOBD全体主集計は再実行していない。

Solによる次工程の設計案: `R2-TARGET-BINDING-DESIGN.md`。現在接続の参照・世代と実車識別/ECU作用範囲の証拠を分け、不足を推定で埋めない。内部APIの実装と現行producerとの契約検証は次工程であり、設計資料の追加を実装完了とは扱わない。

3.13.510 R2保存記録の管理: 検証済みの選択記録を利用者確認付きで1件ずつ削除する。比較候補は解除前に削除せず、確認前後の記録・世代・画面権限を再検査する。DB内でも内容と保存時刻を原子的に照合し、削除transaction完了と事後の不在確認を区別する。不明結果の自動再試行、一括削除、自動整理は行わない。診断結果・保存形式・車両送信は変更しない。保管→閲覧→出力→照合→候補解除→記録整理までを操作可能にする工程であり、対象車両の照合、実行監査、別タブ排他と実車試験は残る。

検証: 保存部108件、実IndexedDBブラウザ24件、消去準備120件、bridge384件、offline180件、session export250件が合格。閲覧・保存・画面遷移のブラウザ試験、1280px/390px表示とSol最終レビューも完了。OBD全体試験は終了コード0を確認（主集計件数は出力回収できず記載しない）。全体試験で見つかったexport試験の依存不足を補い、読取更新時の候補解除→再描画の呼出順も追加検証した。複数タブの同時削除・内容変更競合・中断・不明結果を確認したが、別タブの実行予約を保証するものではない。利用者の実データ削除・実車試験は行っていない。

3.13.509 R2候補の準備状態表示: 照合済みの記録内に「消去前確認」をまとめ、対象未設定・既存モデルの未確認条件を表示する。条件は折りたたみとし、確認済みへ変更する入力は設けない。「比較候補を解除」でページ内の関連付けだけを外し、保管記録・診断結果・一覧・JSON出力・保存成否を保持する。送信、車両操作の取消、保存記録削除は行わない。表示と操作導線の更新であり、実車対応率や消去の安全条件を達成した意味ではない。

検証: 専用ブラウザで既存条件数とラベル全件の一致、初期折りたたみ、解除後の記録・診断・一覧・保存成否保持、追加ストレージアクセスなし、controller遷移なし、明示再照合、読取更新時の表示失効を確認。1280px/390pxと390px実表示のボタン遮蔽検査が合格。保存ブラウザ、画面遷移・詳細59件、保存部85件、消去準備120件、offline180件も合格。Sol設計・コードレビューでの条件数表示の指摘を反映。OBD全体主集計と実車試験は今回は未実施。

3.13.508 R2消去前記録候補の照合: 保存記録を読み込んだ後、現在の読取結果との完全一致を明示操作で確認する。一致時だけページ内の非送信モデルへ記録IDを候補として関連付ける。対象車両・ECU・transportは未設定、保存条件を含む全準備条件は未完了のまま。保存だけでは自動関連付けせず、読取更新・選択変更・ロック・画面移動で失効する。これは記録の内容照合であり、車両同一性・適合・消去許可の判定ではない。実行開始記録、対象との照合、消去workflow全体への接続、実車試験は引き続き未完了。

検証: 保存一覧と保管直後の両経路から照合し、記録ID・全準備条件未完了・非送信を確認。不一致、時刻不正、通信処理中、同一参照内変更、結果差し替え、controller遷移、記録選択変更、保存中の読取変更を検査。Solレビューで検出した古い一致表示の残存と確認開始時の失効順序を修正し、再レビュー済み。保存/閲覧の専用ブラウザ、1280px/390px表示、画面遷移・詳細59件、シリアルlifecycle1020件、消去準備120件、保管85件、JSONポリシー177件・出力238件、offline180件が合格。今回の追加後はOBD全体主集計・実車試験を再実施していない。

2026-09-06 / 3.13.507 統合回帰確認: 隔離Chromeで通常の診断フローをオフライン・多数ライブ値ありで一巡。認証の解除/再ロック、読取結果の取込・出力・再取込、詳細遷移、検索保持、複数ECUと時系列、壊れた/過大な入力の拒否、接続取消・失敗・再試行を確認した。模擬VCIではライブ値なし・回復・途中切断・DTCゼロ・DTC無応答からの復元も合格。ブリッジ384件、PC配布パッケージ336件が合格。すべて模擬またはソフト検証であり、実VCI接続や実車対応率の証明ではない。製品コード・版番号は変更していない。

全体試験で見つかった試験基盤の問題を修正: 4MB保存試験を静的importの他試験より後にawaitし、同期処理が本番相当の期限を消費する重なりを解消。シリアル単体試験には保存記録欄のリセット/再描画依存を補い、ロック時のリセット呼出しも検査する。製品の上限・期限や検査条件は緩めていない。修正後のvalidate:obdとvalidate:r1が合格（保存部85件、シリアルlifecycle1020件・integration318件を含む）。ローカル・公開script.jsもHTTP 200と3.13.507一致を確認した。

3.13.507 R2保管直後の内容確認: 直前に保管を確認できたIDだけを専用ボタンから開く。ランダムID順の一覧を探す必要をなくし、開く際は既存の読取検査を再実行する。一覧由来と保管結果由来の選択を分け、一覧・ページ内容を偽装しない。成否不明・保存中・ロック後には開かず、診断結果・保存形式・車両送信は変更しない。

検証: 専用ブラウザで一覧未取得からの直接読込、二重クリック時の単一読込、一覧状態の不変、元JSONとの出力バイト一致、成否不明・拒否・不正ID・ロック時の読込拒否、返却ID不一致とID変更・ロック後の遅延結果破棄を確認。既存閲覧試験、1280px明色・390px暗色の表示、保管部85件、JSONポリシー177件・出力238件、画面遷移、詳細遷移59件、offline180件が合格。Sol設計・コードレビュー済み。実車試験とOBD全体主集計は今回は未実施。車両消去の実行許可は追加していない。

3.13.506 R2読取結果の端末内保管: 開発・詳細の保存記録欄から現在の読取結果を確認付きで追加保存する。読取・通信処理中、確認後の結果差し替え、ロック・画面変更は開始前に拒否。開始した保存のbusy状態は欄を閉じても保持し、自動再保存しない。保存確認不能時はIDを保持し、重複の可能性への確認後だけ新しいIDでの手動保存を許可する。保管は実車状態・適合・消去許可の証明ではない。削除・保持管理、車両照合、消去workflowとの統合、実車試験は引き続き未完了。

検証: 専用ブラウザで確認キャンセル/例外、セッション差し替え・同一参照内変更・ロック・通信開始後の保存拒否、二重呼出し、閉じ直し後の結果保持、実IndexedDB保存から一覧・読み出し・JSONのバイト一致、成否不明後の確認付き手動再試行、乱数取得失敗を確認。成功と成否不明の表示画像、390pxの確認欄サイズも確認。既存閲覧ブラウザ、保管部85件、事例保存157件、JSONポリシー177件・出力238件、画面遷移、詳細遷移59件、offline180件が合格。Sol設計・再レビュー済み。実車・別タブ排他・ページ終了時の耐久性は未検証、OBD全体主集計と既存診断フロー全体のブラウザ試験は今回は未再実行。

3.13.505 R2保存記録のJSON出力: 開発画面で選択して検証済みの記録を、そのままJSONファイルとして取り出す操作を追加。現在の診断結果から作り直さず、元の記録の文字列をUTF-8で出力する。未選択・読取中・ロック後・一覧外IDでは出力しない。ダウンロード開始と端末への保存完了を区別する。新規記録の作成、削除、診断への取込、車両送信は今回の対象外。

検証: 専用ブラウザで元のJSONとダウンロードしたファイルのUTF-8バイト一致、ファイル名・MIME、URL生成/クリック失敗時の後処理と記録保持、ロック後の出力拒否を確認。1280px明色・390px暗色の表示と横はみ出し確認が合格。保管部85件、セッションJSONポリシー177件・出力238件、画面遷移、詳細遷移59件、offline180件が合格。画面遷移単体試験に既存の閲覧再描画依存が欠けていたため補完した。Sol設計・コードレビュー済み。今回は専用画面と周辺検証に限定し、OBD全体・既存ブラウザ一巡・実車試験は再実行していない。

3.13.504 R2保存記録の閲覧: 開発・詳細画面に既存の消去前記録を探して内容を閲覧する欄を追加。明示操作でID一覧を取得し、選択時に既存の読み出し検査を通して内容を表示する。通常の診断結果へ取り込まず、車両通信・保存・削除・消去許可は追加しない。この保管部への画面からの新規保存は未接続のため、記録を作成していない端末では一覧は空または保管未作成となる。保存対象確認、保持・削除管理、対象車両照合、消去workflowとの統合は引き続き未完了。

検証: 専用ブラウザで空状態、21件のページ送り、本文表示、一覧外ID拒否、ロック・閉じる・モード移動・タブ移動後の遅延一覧破棄、詳細ロック後の遅延本文破棄を確認。1280px明色・390px暗色の画像と文字はみ出しを確認し、一覧高さを240pxに制限。保管部85件、画面遷移、詳細遷移59件、offline180件が合格。既存の模擬VCI読取・JSON保存・オフライン復元・認証を含むブラウザ一巡も合格。Solレビュー指摘の詳細ロックと一覧内ID条件を修正して再レビュー済み。実車・新規保存操作は未検証、OBD全体主集計は未再実行。

3.13.503 R2記録のID一覧: 最大50件ずつ保存記録のIDを取得する内部APIを追加。本文を読み込まず、続きの取得は前ページ末尾のIDを除外して行う。一覧の成功は記録内容の正常性を意味せず、開く際は3.13.502の検査が必要。DB作成・書換え・消去許可への接続はない。通常画面への保存・一覧・内容確認の接続、保持・削除管理、対象車両への対応付けは引き続き未完了。

検証: 保管・読み出し・ID一覧85件、隔離Chromeの実IndexedDB試験22項目、事例保存157件、セッションJSONポリシー177件・出力238件、offline180件が合格。Sol設計・コードレビュー済み。ページ境界、排他的継続、破損本文のID一覧と内容拒否、ページ再開後の一覧を確認。通常画面、実車、ブラウザ/OS再起動後の耐久性は未検証。OBD全体の主集計は再実行せず対象・周辺検証に限定。

3.13.502 R2記録の読み出し: 元のJSONを手元に残していなくても、記録IDから内容を取得する内部APIを追加。読取transaction完了後に形式・バイト列・現行ポリシー・digestを検査し、異常時は内容を返さない。保存済み記録を変更せず、DBがなければ作成もしない。通常画面の一覧・エクスポート・削除・復元操作との接続は未完了であり、利用者向けの保存機能が開放された意味ではない。

検証: 保管・読み出し61件、隔離Chromeの実IndexedDB試験19項目、事例保存157件、セッションJSONポリシー177件・出力238件、offline180件が合格。Sol設計・コードレビュー済み。ページを閉じて開き直す試験を含むが、ブラウザ/OS再起動後の耐久性、実車、通常画面での記録操作は未検証。今回はOBD全体の主集計と画面全体の一巡は再実行せず対象・周辺検証に限定。

3.13.501 R2消去前記録: Sol設計レビューを再実行できたため、独立した端末内保管部を実装する。既存JSONを変更せず追加保存し、書込transaction完了後に別transactionで読み戻し、バイト列等の一致を確認する。重複IDの上書きや、確認不能後の自動再保存はしない。通常画面の保存操作・消去workflow・車両送信には未接続。利用者向けの一覧・削除管理と対象車両への対応付けは後続に残る。

検証: 保管部の障害・境界試験39件、隔離Chromeの実IndexedDB試験13項目、事例保存157件、セッションJSONポリシー177件・出力238件、bridge384件、offline180件、PC起動検証1302件が合格。既存の模擬読取・保存・オフライン復元・認証のブラウザ一巡も合格。Sol再レビュー指摘の保存日時・記録項目・読取時のDBキー構成検査を修正して確認した。今回はOBD全体の主集計は再実行せず対象と周辺に限定。実車、別PC、ブラウザ/OS再起動後の耐久性・保存消失は未検証。

3.13.500 R2受信文字列: 明示した11-bit CANの表示形式だけを扱う独立解析を追加。応答元とSingle Frameの長さからpayloadを取り出し、エコー・別形式・不完全な行・終端異常を正常完了へ変換しない。既存の読取解析、画面、保存形式、送信許可は変更しない。受信受付・評価器との模擬接続を対象とし、実シリアルのコールバックへの接続、消去前の永続記録、実行・再読取は未完了。

検証: 限定解析55件、OBD全体の主集計7447件と同時実行の関連検証、bridge384件、offline180件がエラー0。全体検証開始後に追加した境界・混在ケースは限定解析を再実行して確認。Sol設計・再レビュー済み。ローカルHTTP 200を確認。表示変更がないためブラウザ一巡は再実行せず、実車試験も行っていない。

3.13.499 R2受信受付: メモリ内で試行と接続の同一参照を照合し、古い・別接続の応答を受付前に拒否する部品を追加。不正フレームと上限超過を保持し、後から正常完了へ戻さない。終了後の追加・再終了も拒否する。実通信コールバック、永続記録、送信経路、画面には未接続。実機の再接続試験を完了した意味ではない。

検証: 受信受付160件（128件上限の逐次確認を含む）、応答評価47件、準備120件、表示85件、bridge384件、offline180件がエラー0。結果不明でも取得済みの評価を保持する修正と、フレームを一度だけ検証・コピーする修正を反映しSol再レビュー済み。今回は対象・周辺検証に限定し、OBD全体とブラウザ一巡は再実行していない。

3.13.498 R2応答評価: 正規化済みの合成応答を対象に、期待元別の肯定観測・不明応答・無応答と、予期しない応答元を分離する非送信APIを追加。途中終了・混在応答・一部無応答を候補集合の完了へ変換しない。生フレーム解析、試行との紐付け、監査保存、実行transport、画面表示には未接続であり、実消去の成立を確認したものではない。

検証: 応答評価47件、準備モデル120件、操作表示85件、bridge384件、offline180件がエラー0。Solレビュー指摘の可変アクセサ入力を拒否し、確定値のスナップショットを使う形へ修正・再レビュー済み。全体OBDおよびブラウザ一巡は今回は再実行せず、独立追加APIと周辺に絞った。実車試験なし。

2026-09-06 R2通信根拠確認: `R2-ELM327-PROTOCOL-EVIDENCE.md` に公式ELM327DSLの参照箇所と実装境界を記録。Mode04の肯定応答だけで全ECU完了・修理完了を断定せず、機能宛てと単一ECU指定の違い、書込開始後の結果不明、自動再送禁止を次工程の受入項目にした。現在のtarget.ecuIdは送信範囲の証明ではないため、通信へ直結しない。資料・設計のみの更新でアプリは3.13.497のまま。

3.13.497 R2現在状態管理: メモリ内のcontrollerが最新スナップショットだけを受け付ける。確認前や取消前の古い参照、JSON複製、別controllerの参照を拒否し、検証中の再入を遮断する。失敗時は状態を維持し、次の正常操作は可能。純粋APIは変更せず、controllerにも送信・認証・保存機能は持たせない。画面との接続、別タブ排他、実消去は後続工程。

検証: モデル120件、OBD本体7447件、offline180件がエラー0。Sol設計・実装レビュー済み。今回UIは版番号以外を変更せず、実車およびブラウザ一巡の再試験は行っていない。

3.13.496 R2取消: 非送信の消去準備モデルに終端状態 `cancelled` を追加。取消後のモデルから更新・再確認・再要求はできず、確認と影響承認を解除する。対象・消去前記録参照・事前条件は残す。UI操作、保存形式、車両通信には未接続。古いスナップショット全体を失効する権限管理ではなく、実行時の一元管理は後続工程に残る。

取消追加の検証: 純粋モデル99件、bridge384件、offline180件がエラー0。実装時のOBD全体検証も合格し、その後は本体を変更せず入力不変性等のテストのみ補強。Sol設計・レビュー済み。実車通信中の取消、画面からの操作、実消去は未実装。

3.13.495 R2開発表示: 開発資料のDTC消去項目に、初期モデルの対象未設定・消去前記録参照なし・未完了12条件を折りたたみ表示。接続中車両の評価とは明示的に区別し、確認入力・状態遷移・送信・保存への接続は追加しない。通常診断画面への項目追加なし。表示回帰85件と隔離ブラウザ1280/390pxで展開・収納・横幅・無効ボタン・認証再ロックを確認。実車試験ではない。

3.13.494 R2非送信モデル: 消去前セッション・車両/ECU/transport参照・事前11条件を管理する2つの純粋APIを追加。明示確認で12条件目を記録し、対象/記録/条件更新で確認を失効する。重複条件、不整合な確認済み状態、安全フラグ改変、古いrevision、更新番号の上限、同じ確認での再要求を拒否し、対象と条件は不変スナップショットとして保持する。到達範囲は `dispatch_blocked` までで、実送信・UI・セッション保存への接続はない。設計と実装レビューはSol、実装はTerra。R2の実行・再読取・実車試験は引き続き未完了。

検証: 新モデル53件、OBD全体7447件、bridge384件、offline180件がエラー0。認証、模擬VCI読取、無応答からの復帰、保存・オフライン復元のブラウザ一巡も合格。実車通信なし。

2026-09-06 認証操作の追加検証: `validate-scanner-access.cjs` を通常のブラウザ検証から呼び出す。隔離contextで配信するJSの照合値1か所だけを試験用SHA-256へ差し替え、本体の照合関数・解除イベント・再ロック処理を1280/390pxで実行する。ボタンとEnterの両方で解除、入力欄消去、セッション内の解除状態、通常画面から開発画面への移動と再ロック、再読込後のロック維持を確認。Service Workerを無効にして試験用JSをキャッシュせず、contextを終了する。元ファイル、公開設定、利用者のパスワードは変更しない。実パスワードでの利用確認や、認証方式全体のセキュリティ監査を完了した意味ではない。

2026-09-06 認証入口の確認: 既存の隔離ブラウザ試験に、解除済みfixtureを設定する前の初回ロック・誤パスワード拒否・再読込後のロック維持を追加。保護領域と車両設定が非表示、解除キー未保存、誤入力後に再試行可能、再読込でパスワード入力が空になることを確認する。その後の模擬読取は従来どおり解除済みfixtureで行い、正しいパスワードによる解除試験とは区別する。利用者のパスワード・ブラウザプロファイルは使用しない。認証処理や診断処理の変更はない。

PC配布確認（3.13.493）: `validate:workstation` の古いnpm一覧検証を、追加済みのブラウザ・フィルター・再生試験と連結コマンドに追従させた。起動検証1300件、配布検証336件に合格。現行資材から一時フォルダーへ実パッケージ846ファイル（整合性一覧845ファイルと一覧自身）を生成し、そのフォルダーの同梱依存だけで一時ポート起動、本体/JS/Service Worker/マニフェストのHTTP 200・版一致、両サーバー終了を確認した。既存3001番のサーバーは変更していない。VCI列挙入力は空として実車通信なし。これは同一PCでの配布資材確認であり、別PCのNode導入・実ブラウザの利用者認証・PC再起動の完了を意味しない。

以下は現在の確認状況。下部の版別作業記録にある「未実施」「次の修正」は、その版時点の履歴であり、現在の残作業一覧ではない。

| 操作・条件 | 現在の根拠 | 残る確認 |
| --- | --- | --- |
| 車両選択・基本結果・個別データ・戻る | 隔離ブラウザで選択保持、詳細遷移、検索保持を確認 | 実タブレットのタッチ操作、利用者による操作評価 |
| 接続・基本読取・切断 | 模擬VCIでDTC/FF/PID/readiness/ECU情報等を実際の画面経由で取得 | 所有VCIと車両での通信・適合確認 |
| 正常0件・無応答・取消・通信障害 | 別状態として表示、要求停止、取得済み情報保持、接続再試行をブラウザで確認 | 実VCI抜去、車両応答遅延などの実機差 |
| 保存・再読込・オフライン | 通常/部分取得/無応答のJSON保存と空画面からの復元を確認 | PC再起動、配布先環境、認証を含む利用者経路 |
| ライブ値の選択・基本グラフ・記録確認 | 模擬60点と複数ECUの記録、選択、再生、保存復元を確認 | 長時間の実車取得、性能・端末差 |
| ブラウザ再起動 | 3.13.513でサーバー停止・ネットワーク無効のままキャッシュ起動、記録復元と画面一巡を再検証 | 自動操作環境の再ダウンロードでブラウザ終了する問題は未解決。手動操作・OS再起動は未検証 |

R1の模擬一巡は成立しているが、実機利用完了とはしない。次のソフト側優先は、配布先での起動・オフライン・認証の一巡と、通常/開発画面の利用者評価に向けた整理。実機確認はソフト試験と別に残し、模擬試験を増やして実車対応率へ換算しない。R2以降の順序は下記計画を維持する。

追加回帰試験: DTC無応答の保存・復元直後に正常な模擬VCIへ再接続し、DTC2件・1000rpm・保存/保留/永久の取得状態が復帰することを確認。NO DATAと未完了試行が0へ戻り、JSON保存・オフライン再読込後も同じ事実と表示を保持した。本体は3.13.493のまま変更なし。

## 保存DTC無応答の判定修正（3.13.493）

保存DTC要求03へのNO DATAを正常0件として扱わず、未完了・未取得として保持する。保存DTCの取得確認ができない場合は、後続のプロトコル確認と保留・永久DTC要求へ進まない。正常な43の0件応答、既存の07/0A等の扱い、保存形式、車両送信の許可範囲は変更しない。模擬応答から保存・オフライン復元まで検証する対象であり、実車適合の確認ではない。

## DTC0件の応答と表示（3.13.492）

保存・保留・永久DTCがすべて0件の模擬正常応答をブラウザ試験に追加。前回のDTCが残らず、ライブ900rpm・NO DATA0件・未完了試行0件を保持して保存・オフライン復元できることを検証する。DTC欄のempty表示「応答0件」は通信無応答と紛らわしいため「コード0件」へ変更する。カバレッジ計算、empty分類、診断、保存形式は変更せず、車両全体の正常や全項目の取得完了を意味しない。実車確認ではない。

## 基本読取結果の読みやすさ（3.13.491）

8項目がPCで横一列になり細くなる結果一覧を、4列/2列/1列の画面幅別配置へ変更。項目名・値・内訳・取得状態の文字を拡大し、全8項目と詳細への操作は維持する。1280/768/390/320pxの明暗表示で列数・全情報保持・文字のはみ出しと操作領域を検証する。診断・通信・保存処理は変更しない。

## 空の保存通知帯の整理（3.13.490）

結果画面の保存通知が空の場合だけ余白・枠・背景をなくし、何も通知していない帯が残る表示を修正。role=statusの通知欄やメッセージ設定処理は維持する。隔離ブラウザでURL生成失敗を注入し、エラー表示・元セッション不変・通常の保存への再試行・成功通知を検証する。保存・診断・通信処理への変更はなく、CSSのみの表示修正。

## 途中取得結果の保存・復元（3.13.489の追加検証）

通信エラーで中断した読取結果を通常画面からJSON保存し、空の別タブでオフライン再読込する試験を追加。DTC2件・ライブ値0件・読取実行要約全体が一致し、失敗件数1件・通信エラー1件と「一部取得」表示が保持されることを確認する。元タブは再読み込みせず、そこからの再接続・900rpm取得試験も維持する。本体・保存形式・診断処理への変更はなく、実機通信の確認ではない。

## 通信エラー後の再接続復帰（3.13.489）

3.13.488で再現した過剰な終了保留を修正。同じreader/portのread失敗をWeakMapへ記録し、cancelがその同じエラーを返す場合だけ既知のストリームエラーとして扱う。記録は終了処理で消費し、デコード失敗や別readerの遅着をこの扱いに含めない。書込完了待ち、両ロック解放、port.close成功の条件は変更せず、任意のcancel失敗やclose・ロック解放失敗は引き続き保留する。診断値・保存形式・車両要求の許可範囲は変更しない。

実ReadableStreamを用いた終了待ち/別取消エラー/両ロック解放失敗/close拒否の追加試験に合格。ブラウザではDTC取得後の0101通信障害→後続要求停止→取得済みDTC保持→終了成功→再接続→900rpm取得→保存・オフライン復元まで確認した。実VCIの抜去や復帰の確認ではない。

## 読取途中のストリーム障害（3.13.488の追加検証・修正前の記録）

通常画面からの基本読取で、DTC取得後の0101要求時にブラウザ内の模擬ReadableStreamをエラー終了させる。後続要求が出ず、取得済みDTC2件は保持され、前回ライブ値は残らないことを確認。エラー状態のreader.cancelも失敗するため既存制御は「終了未確認」を保持し、再接続を禁止する。終了処理がsettledした後の保存ボタン有効化も確認するが、この障害ケースの実ファイル保存・再読込は本試験には含めない。通常の切断成功、手動中断、実VCIの抜去・復帰とは区別する。本体や安全ゲートへの変更はない。

Solレビューで復帰上の不具合を指摘: 既にerroredのReadableStreamはcancelも同じエラーでrejectするため、両ロック解放とport.close成功後まで再接続を禁止するのは過剰な保留となる。現試験はこの既知制限を記録するもので、望ましい復帰仕様の完成証拠ではない。次の修正条件は、同一reader/portで実際にreadが失敗した証拠を区別して保持し、書込settled・両ロック解放・close成功がすべて確認できた場合のみ切断完了へ進めること。任意のcancel失敗、close拒否・未完了、ロック解放失敗は引き続き保留し、旧readerの遅着や保存証拠保持も回帰検証する。まだ本体修正は未実施。

## 読取後の車両選択ブラウザ検証（3.13.488の追加検証）

読取記録を開いた通常画面から車両設定へ戻り、メーカー→車種→型式→選択可能な年式・エンジン型式を実ブラウザで順に選択する試験を追加。1280/390pxで設定画面の維持、先に選んだ項目の保持、取込済みセッションの不変、メーカー解除時の連動項目クリアと結果画面への復帰を確認する。本体への変更はなく、車種適合や実車通信の確認ではない。

## DTC検索のタッチ操作統一（3.13.488）

DTC検索件数をライブ検索と同じ小さな表示へそろえ、共通の検索解除ボタンを48pxへ拡大。DTC本文・注意事項・取得情報は省略しない。320/390/768/1280pxの明暗表示で検索欄の重なりとボタン寸法を確認し、検索条件の画面往復保持、解除後の全件表示・件数・入力フォーカス復帰を検証する。診断・保存・通信処理への変更はない。

## ライブデータ操作欄の整理（3.13.487）

一覧表示・選択のみ・選択件数・選択解除を2列2段へ整理し、検索件数の帯を小さなテキストへ変更。タッチ操作の48px領域は保ち、測定値の手前にある操作欄を低くする。320/390/768/1280pxの明暗表示で重なり・はみ出し・2段以内を検証し、検索・選択・保存復元の既存試験を維持する。表示のみの変更で診断ロジック・取得値・保存形式・通信許可は変更しない。

## 応答なしからの再読取復帰（3.13.486の追加検証）

模擬VCI試験を1726rpm取得→NO DATA→800rpm取得の3回へ拡張。通常画面の再接続で新しい値へ更新され、前回値やライブ0件表示が残らず、NO DATA件数・取得未完了件数が0へ戻ることを照合する。各回の保存・切断・オフライン復元と21件の読取コマンドも検証対象。本体・診断・保存形式・通信許可への変更はなく、実車での復帰確認とは区別する。

## 正常読取後のライブ応答なし（3.13.486）

通常画面で1726rpmを取得・保存・復元した直後に、同じDTC等を返しつつPID 0CだけNO DATAとなる模擬VCIへ再接続する試験を追加。前回RPMが混入せずライブ値0件、DTC・readiness・ECU情報は保持されることを確認する。NO DATA 1件・取得未完了1件を明示的に照合し、保存→切断→空の画面からオフライン復元後も状態と表示が一致するか検証する。画面確認で0件時に入力形式の誤りを示唆する文言を発見し、表示可能な値がないという事実だけを伝える文言へ修正。診断ロジックや保存形式への変更はない。実機の通信品質や車種適合を証明するものではない。

## 通常画面の模擬VCI一巡（3.13.485の追加検証）

ブラウザ内だけのReadableStream/WritableStreamをVCIとして、通常画面の接続→基本読取→JSON保存→切断→空の画面からオフライン再読込を試験する。既存の内部通信試験と同じ合成応答を用い、DTC P0133/P0420、回転数1726rpm、readinessとECU情報の取得状態を照合。実行するコマンド21件を完全一致で検証し、ポート開始・終了・読取取消各1回とロック解放も確認する。

ファイル取込だけではなく実際のアプリ通信処理を通るR1の正常系ブラウザ試験として追加。本体・版番号・通信許可は変更しない。実VCI適合の確認ではない。既知の再起動後ダウンロード問題と混同しないよう、この正常接続試験は `--restart` では除外し、通常のオフラインCIで実行する。

## 接続待機中の操作状態（3.13.485）

VCI選択待ちでも接続ボタンが有効のまま残る不具合を、応答を保留する模擬VCIで再現した。Sol設計・レビューに基づき、選択待ち/ポート開始待ちで通知と接続ボタンだけを同期更新する小さな表示関数を追加。画面全体の再描画や自動画面遷移は呼ばず、既存の重複取得ガード・通信・状態管理・保存処理は変更しない。詳細画面の接続ボタンも待機中は無効にする。

保留中の状態表示・ボタン無効化・取得回数不変・取消/失敗後の再試行をブラウザで確認。R1回帰検証は合格し、Sol最終レビューで安全/回帰の指摘なし。実VCIは使わず、模擬書込み0回のまま確認した。

## 接続取消・開始失敗の通知保持（3.13.484）

通常画面でVCI選択を取り消すと、再描画で取消通知が一般案内へ上書きされる不具合を実ブラウザ試験で再現した。Web Serialの未接続状態で、選択取消・選択失敗・ポート開始失敗の通知を既存の理由から表示する。ロック、非対応環境、終了未確認の表示を優先する順序は維持し、接続・送信・診断・保存の処理は変更しない。

隔離ブラウザ内の模擬VCIで、通常ホームから接続へ進み、選択取消、open失敗、再び取消の3回を実行。理由表示、再試行ボタン、未接続表示、車両画面への戻りを確認する。requestPortは3回、openは1回、writeは0回。実VCIの正常接続や切断試験の代用にはしない。既存のオフライン一巡CIに登録し、画面画像も保持する。

## 記録一覧からの直接選択（3.13.483）

ライブ推移の一覧行をボタン化し、時刻・値を押すとその記録位置へ移動する。既存スライダーのinput経路を使い、再生中は停止する。Enterキーでの選択にも対応し、ECU・項目・時刻・値を読み上げ名に含める。48px以上の操作領域、320/768pxの明暗表示、先頭/末尾選択、再生停止、保存復元をオフライン実ブラウザ試験で確認した。計測値・診断・保存形式・車両通信は変更しない。

## 記録一覧と選択位置の同期（3.13.482）

ライブ推移の時刻・値一覧で、グラフの選択行だけを強調しaria-currentを付ける。一覧を開く操作、スライダー、前後送り、再生に合わせて選択行を一覧内へ表示する。スクロール対象は一覧だけで、ページ全体や他の系列は移動しない。閉じている一覧はスクロールせず、手動で一覧を閲覧する操作も維持する。

320/768pxの明暗表示で選択行の可視範囲と一意性、先頭・末尾への移動、ページ位置不変、保存復元をオフライン60点試験で確認。計測値、診断ロジック、保存形式、車両通信は変更しない。

## タッチ端末での記録送り（3.13.481）

ライブ推移に48px以上の前/次ボタンを追加し、密な記録でも1点ずつ移動できるようにした。既存スライダーのinput経路を使うため、再生中の操作は再生を停止する。先頭・末尾で範囲外へ進まず、境界に達してもボタンのキーボードフォーカスを保持する。計測値・統計・診断・保存形式・車両通信は変更しない。

320/768pxの明暗表示、前後移動、境界、フォーカス、再生停止、保存復元を実ブラウザのオフライン60点試験で確認。再生単体36件、オフライン更新180件も合格。R1の記録確認操作の改善であり、実VCI対応の追加ではない。

## R1ブラウザ試験の自動化（3.13.480の検証基盤）

`npm run validate:scanner-browser` を追加し、既存のオフライン・60点再生・複数ECU・保存復元・読込障害試験をGitHub Actionsの独立ジョブへ登録する。Playwright 1.62.1とChromiumはrunnerの一時領域にのみ導入し、アプリの依存関係や実車通信は変更しない。合成データのスクリーンショットだけを7日間保持し、ブラウザプロファイルは添付しない。初回CIの実行結果は別途確認し、ジョブ登録だけでは合格と扱わない。ブラウザ再起動後の再ダウンロード問題と実端末・実VCI検証は対象外のまま残る。

## 読込取消・拒否後の記録保持（3.13.480の追加検証）

R1のファイル操作試験へ、置換確認の取消、空選択、空ファイル、未対応形式、2 MBを超えるテキスト、壊れたJSONを追加。DTC本文だけでなくセッション全体・解析入力の不変と保存ボタンの再利用を確認し、その後に通常のファイル選択から正常な記録を再読込する。本体・保存形式・診断・版番号は変更しない。

追加で、隔離ブラウザ内のFileReaderへerror/abort/readAsText例外を注入。読込中の保存停止、失敗後の保存復帰、選択ファイルのクリア、セッションと解析入力の保持を確認した。失敗後にonload/onerror/onabortが遅着しても記録・通知を変更しない。各試験後に本来のFileReaderを復元し、通常ファイル選択での復帰、60点再生、複数ECU保存復元までオフライン一巡が合格。実際のストレージ故障や実端末のOSダイアログを再現した証拠ではない。

置換取消は実ブラウザの確認ダイアログを操作する。空選択と形式別拒否はブラウザのfile inputへ設定して検証し、OSのファイル選択ダイアログ自体の取消試験とは区別する。繰り返しのpicker操作で自動試験がタイムアウトしたため、形式別拒否はinput経路で分離した。ネイティブpickerの反復操作は別途実端末確認が必要であり、アプリの不具合とも正常とも断定しない。

## タッチ端末での再生不可理由（3.13.480）

再生無効時の理由をマウス用ツールチップだけでなくボタン内にも表示する。記録点不足、時刻不明、順序不整合、30分超過を区別し、読み上げにも含める。再生条件・診断・保存・車両通信は変更しない。R1の操作性改善であり、実機対応範囲の拡大ではない。

## ライブ記録の再生・一時停止（3.13.479）

各ライブ推移へ再生/一時停止ボタンを追加。既存pointsの取得時刻差に沿い、表示位置だけを進める。点間で一時停止した経過時間を保持して再開し、末尾で停止、末尾からの再生は先頭へ戻す。単一系列のみ再生し、他系列を含む手動位置変更で停止する。値の補間、統計・診断・保存・車両要求への変更はない。

Sol設計/レビューに基づき、有効で厳密増加の時刻かつ総時間30分以内を再生対象とする。長時間・欠損・逆転・重複時刻は再生無効とし、手動スライダーと記録一覧は維持する。上限は今回のUI再生範囲で、読取や保存の上限ではない。画面/タブ変更、詳細の閉鎖、再描画、非表示、pagehideで停止し、rAFと監視listenerを解除する。画面復帰で自動再生しない。

仮想時計でpause残時間、異常時刻、交差系列、停止時解放を検証しR1に登録。実ブラウザでは60点再生、一時停止、画面移動、詳細閉鎖、保存復元を確認する。詳細メニューの旧7分類固定テストがライブ推移追加に追随していなかったことも発見し、全8対象IDの検証へ修正してR1へ登録した。実車計測や長時間連続記録の完成を示すものではない。

## 選択解除後の操作継続（3.13.478）

「選択した項目のみ」でキーボードから項目を外すと、行と一緒にフォーカスが失われる不具合をブラウザで再現した。操作中の行が隠れる場合に限り、次の表示項目、なければ前の表示項目、どちらもなければ検索欄へ移す。最後の選択解除では従来どおり全項目へ戻る。選択・検索条件・取得データ・保存・診断・通信は変更しない。

修正前に失敗したSpaceキーでの連続解除を回帰試験へ追加。単体試験には次/前/検索欄への復帰と選択数不変を追加した。新機能ではなく、R1の連続操作を妨げる表示上の不具合修正。

## 複数ECUの表示・保存一巡（3.13.477の確認）

架空のECU 7E8/7E9が同じPID 0Cを異なる回転数で返すケースを `validate-scanner-file-flow.cjs --live` へ追加。ECU別表示、7E9だけの選択、各グラフの独立した時点移動、選択した状態で全データをJSON保存し再読込して同じ操作を行う経路を確認する。単一ECUだけでは検証できなかった、同一PIDの所属保持をブラウザ経由で確認する試験。アプリ本体・版番号は変更せず、実車の複数ECU接続対応を証明するものではない。

## ライブ推移の時点選択（3.13.477）

各グラフに記録位置スライダーを追加。同じrow.pointsの指定位置から時刻・値を表示し、対応する棒を強調する。キーボードの先頭/末尾/1点移動とポインター選択に対応し、読み上げ用にも時刻と値を設定する。統計・比較条件・元データ・保存形式・車両通信は変更しない。位置は再描画時に末尾へ戻す。これは記録の手動確認であり、自動再生・連続計測の実装ではない。

架空60点で先頭/末尾/隣接点と値の一致、画面幅・明暗別の位置選択、記録一覧との一致、保存後の再読込をブラウザ検証する。R1の記録確認操作を進める更新であり、実VCIでの試験完了ではない。

## ライブ値の複数選択（3.13.476）

各計測項目のチェックと「選択した項目のみ」「選択解除」を追加。検索と選択を併用でき、同じPIDでもECU別に選択する。最後の選択を外すと選択のみ表示を解除し、検索に一致する項目へ戻す。画面移動では保持し、読取データが再描画されると選択を同期的に解除する。継続的なポーリング中の選択維持ではなく、現在のスナップショット表示に対する選択機能。

表示を絞っても取得総数・注意・元データ・保存対象・診断入力・車両要求は変更しない。共用検索はmonitorだけの追加オプションとし、DTCは既存経路を維持。Sol設計指摘に基づき、0件文言・change通知・再描画時解除・ECUを含む選択名を反映した。monitor43件、DTC126件、R1回帰とブラウザ明暗8条件、画面復帰、絞込中でも全計測値を保存/再読込する試験を追加・確認した。

ブラウザ画像で共通inputスタイルによるチェック欄の肥大化を検出し、20px固定と48px以上のラベル領域へ修正。項目名の横幅とチェック欄寸法も検証する。Sol最終レビューで指摘なし。monitor/DTC検索テストを `validate:readout-filters` としてR1へ登録し、今後も選択と共用検索の回帰を検証する。実車対応の追加や複数PIDへの送信制御ではない。

## ライブ値の一覧表示（3.13.475）

通常のライブ値に「一覧表示」チェックを追加。既存カードの同じDOMをCSSだけで一覧へ切り替え、項目名と数値を横並びにし、ECU/分類と注意文も保持する。検索処理・計測値・診断・通信・保存形式は変更しない。別データ画面から戻った際は検索と表示形式を保持する。表示形式は現在の画面内だけの設定であり、ファイルや再起動後への永続保存は追加していない。

架空の回転数・水温で320/390/768/1280pxの明暗8条件、キーボード切替、切替前後の本文一致、注意文の表示、検索中の往復、検索解除、JSON保存と再読込をブラウザ確認した。複数項目の任意選択、継続ポーリング、実車での計測確認はこの変更には含めない。

## 開発資料の分類表示（3.13.474）

開発資料を、機能の開発予定・読取要求と通信条件・対応機器と接続方式・診断データの対応範囲・ブリッジ通信仕様・読取保護の内部条件の6分類へ整理した。一度に1分類だけ展開し、閉じて戻っても選択分類を保持する。重複する英語見出しと装飾枠を除去。既存10個の描画先と全資料内容は保持し、進捗計算・通信・保存形式には変更なし。読取操作の分類とは独立し、接続状態・切断ボタンは資料外に保持する。

隔離Chromeの明暗・画面幅8条件でキーボード開閉、排他展開、操作分類との独立、再展開時の保持を確認。アプリ本体を読み込むブラウザ試験でも全描画先に資料が入ること、通常診断に戻ると資料が隠れること、保存・再読込の一巡を確認した。実車試験や開発予定機能の完成を意味しない。

## 開発読取の接続方式別整理（3.13.473）

5つの読取分類内を「PC直接接続 / ELM327」と「ローカルブリッジ」に分け、ボタン名から重複する接続方式表記を除去。両方式を隠さず比較でき、タブレット幅では横並び、狭い画面では縦並びにする。fieldset/legendでボタンの所属も保持する。ID・ハンドラー・無効状態・ロック・切断・測定条件・診断計算・保存形式は変更しない。

通信ハンドラーを読み込まない隔離Chromeで320/390/768/1366pxの明暗8条件を確認。全ボタンの方式別所属と無効状態、分類切替、測定条件保持、ロック時フォーカス、タッチ寸法、はみ出しを検証した。実VCI接続の確認ではなく、R1の開発画面整理として記録する。

## ライブ推移の記録一覧（3.13.472）

各ライブ推移に「時刻と値」の開閉可能な一覧を追加。秒・ミリ秒・タイムゾーンを表示し、タッチ操作でも各点の記録値を確認できる。元のグラフと同じpointsを使用し、計測値・比較条件・グラフ計算・保存形式は変更していない。60点のグラフが狭い画面でも収まるように調整し、長い一覧はスクロールで確認する。

架空の60点を使い、一覧の開閉、数値・取得時刻、キーボード操作、スマホ幅のはみ出し、JSON保存後の再読込を検証。前回の数値入り試験は再読込時の置換確認が自動キャンセルされても旧グラフで合格する余地があったため、2回目の確認にも明示回答し、新しく生成された一覧が閉状態から表示されることを検証へ追加した。前回の同一グラフ比較だけでは再読込成功の証拠として不十分だった点を訂正する。

## 再起動後保存の切り分け（2026-09-05）

`scripts/reproduce-browser-download-restart.cjs` は診断ツール・サービスワーカー・車両APIを読み込まず、架空のJSONをBlobで保存するだけの最小再現。新規の一時プロファイルでは初回保存に成功するが、同じプロファイルを再起動して再び保存するとChromeが終了する。オンライン、別ダウンロード先、別Nodeプロセス、終了前2秒待機でも再現。`--no-first-download` で初回ダウンロードを省いた対照試験は、再起動後の保存に成功した。したがって診断データ・オフライン処理を原因とは扱わず、ダウンロード済みブラウザプロファイルの再利用に関係する試験環境側の問題として記録する。内部原因と通常の利用者ブラウザでの再現性は未確定。

再現: `PLAYWRIGHT_MODULE` に既存Playwrightモジュールを指定し、`node scripts/reproduce-browser-download-restart.cjs`。比較オプションは `--online`、`--separate-process`、`--settle-before-close`、`--no-first-download`（個別使用）。既定試験は上記環境で失敗終了する再現用であり、リリース合格用の検証へは登録しない。ユーザーのプロファイル・ダウンロード履歴は変更しない。プロファイルと架空出力はOS一時フォルダーへ作成する。ブラウザ更新後は同じ最小再現と `validate-scanner-file-flow.cjs --restart --live` の両方で再確認する。

## ライブ推移の個別表示（3.13.471）

追加データの「ライブ推移」から既存の時系列グラフだけを開けるようにした。計算・比較条件・保存形式は変更していない。時系列がない場合は既存の詳細なし表示へ戻る。現状は静的グラフであり、再生/一時停止や時点指定の操作は未実装。

既存のコア関数で架空の保存ファイルを生成し、回転数・冷却水温のライブ値、rpm検索、回転数800→1000rpmの2点グラフ、JSON保存と再読込後の同じグラフをブラウザで確認。途中の0項目表示はテストブラウザが置換確認を自動キャンセルしたためで、データ消失ではなかった。確認操作を試験へ明示し、Solでもコアの保存往復と表示IDの対応に問題がないことを確認した。実車・実機の計測試験ではない。

数値入り試験は `validate-scanner-file-flow.cjs --live`、オフライン再読込との組み合わせは `--offline --live` で実行する。両方合格。`--restart --live` はグラフ表示後のJSONダウンロードで試験ブラウザが終了するため未合格。Chrome 152.0.7977.65でプロセス終了コード3221225477を記録し、Edgeでも同段階の終了を再現した。アプリのpageerrorは0件だが、原因と実端末での再現性は未確定。従来の `--restart` は保存済みファイルを開く検証であり、再起動後の新規ダウンロード成功の証拠ではない。この制約をR1の未解決項目として残す。別ブラウザでの再現には `PLAYWRIGHT_CHANNEL=msedge` を指定できる。

## 開発画面の解除後表示（3.13.470）

解除済みの開発画面ではパスワード入力欄を非表示にし、ロックボタンを見出し側へ移動。解除操作が無効かつロック操作が有効な状態だけに限定し、確認中や再ロック後は入力欄を保持する。認証処理・読取権限・切断処理は変更しない。画面8条件で解除前/確認中/解除後/再ロックの表示、ロックへのフォーカス、状態表示の保持を確認した。認証そのものの実機試験ではない。

## 開発画面の分類切替（3.13.469）

開発・詳細の5つの読取分類は一度に1つだけ展開する構造へ変更。別分類を開くと以前の分類が閉じ、長いボタン列の積み重なりを抑える。ネイティブdetailsのグループ機能を使用し、対応しないブラウザでも従来の個別開閉を維持する。通信設定・測定条件・技術情報は独立し、エラー・状態表示と切断操作は分類の外に保持した。

320/390/768/1366px、明暗8条件の隔離Chromeで5分類の排他展開、キーボード操作、測定条件・同一車両確認の保持、各分類のボタン寸法とはみ出し、ロック時のフォーカスを検証。通信ハンドラーは読み込まない表示試験であり、読取権限・診断・保存形式は変更していない。

## UI確認追記（3.13.465）

2026-09-05、公開版 `https://tool.mukiguri.com/` のChromeで確認。実装コミットは `db910121`。下記は画面操作の確認であり、実車対応や通信機能の完成を示すものではない。

- 詳細表示でトヨタ -> プリウス -> ZVW30 -> 2015年を連続選択しても読取設定に留まる。
- 診断ホームへ戻り、通常表示の車両選択を開いても上記4項目を保持する。
- 通常表示で年式を手入力へ切り替えると入力欄が現れ、読取設定に留まる。
- 通常表示のホームから開発・通信設定を開くと、詳細読取メニューへ移動する。
- 検証用の車両入力は未選択へ復元した。確認時のブラウザエラーログは0件。
- 別途、通信処理を読み込まない表示専用ブラウザ試験では320/390/768/1366pxの明暗8条件で、条件保持・折りたたみ・フォーカス・はみ出しを検証済み。

残る確認: 実タブレットの操作感、実機VCI接続中の一巡、署名済みiPhoneアプリでの実機試験。UI整理だけを理由に診断機コアや車種対応の完成率は引き上げない。

## R1の回帰検証入口（2026-09-05）

### 保存結果から個別データへの一巡（3.13.470の確認）

ファイル操作の実ブラウザ試験を拡張し、全体結果からライブ値、追加データの6分類、DTC一覧、全体結果への復帰を1280/390pxで確認。DTCのみの架空保存結果にライブ計測値が生成されないこと、追加データは選択項目だけを表示し未収録時は詳細なしを表示すること、元のDTC結果を保持することを検証した。DTC検索P0420を残したままライブ値へ移動して戻り、検索条件と絞り込みが維持され、解除で全件へ戻ることも確認した。

通常の隔離ブラウザとオフライン完全再起動後の一巡に合格。ソフト側の画面遷移確認を進めたもので、新しい診断機能・実車対応・認証試験の完了ではない。実計測値を含む記録のグラフ再生、実タブレット、実VCIでの操作確認は残る。アプリ本体は変更せず、版番号は3.13.470を維持する。

### ブラウザ完全終了後のオフライン復元（3.13.468）

専用の永続ブラウザプロファイルで資材保存後にサーバーを停止し、Chromeを完全終了して同じプロファイルで再起動する `--restart` 試験を追加。修正前は診断データ読み込みが20秒を超えて継続した。診断JSONのネットワーク優先取得に待ち時間の上限がないため、Sol設計・レビューに基づき成功応答の本文取得まで15秒の上限を設定した。タイマー処理が遅延した場合も経過時間で判定し、失敗時は現在版のキャッシュのみへ復帰する。HTTPエラーは本文を解析せず従来の状態判定へ渡す。保存禁止・保存失敗・キャッシュなしの扱いは維持した。

最終版の隔離Chrome再起動試験では約16秒でデータ読み込み完了。サービスワーカー応答と未保存URLの通信失敗を確認し、保存JSONの復元、壊れたJSONのエラー表示と元結果保持、戻る操作に合格した。サーバー停止だけで通信模擬を使用しない条件でも同じ一巡に合格。通常オンライン相当の隔離試験と `validate:r1` も合格し、オフライン単体は180項目・エラー0。

R1の「ブラウザ再起動後に保存記録を開く」はソフト試験済みへ更新する。利用には事前のオフライン資材保存が必要。PC本体の再起動、実タブレット、利用者認証、キャッシュ消失後の復旧、実VCI接続の完了を示すものではない。診断ロジック・保存形式・車両送信許可は変更していない。試験画像と出力はOS一時フォルダー内に保持する。

### 保存ファイルのブラウザ一巡（3.13.467）

`scripts/validate-scanner-file-flow.cjs` でアプリ本体のHTML/JS/CSSを読み込み、画面のファイル選択とダウンロードを検証した。架空native archiveの保存DTC P0300・保留DTC P0420を読込、JSON保存、ページ再読込、結果が空であることの確認、保存JSONからの復元、壊れたJSONのエラー表示と元結果保持、結果画面へ戻るまで合格。エラー表示は1280pxと390pxのスクリーンショットでも確認した。

隔離した解除済みテスト環境を使用し、ユーザーのブラウザデータ・パスワードは使用していない。車両APIと外部通信は遮断。サービスワーカーも無効のため、この結果を認証試験・オフライン再起動試験・実車試験の完了とは扱わない。ブラウザ例外と予期しない通信要求は0件。

実行: Playwrightを利用できる環境で `node scripts/validate-scanner-file-flow.cjs`。必要なら `PLAYWRIGHT_MODULE` で既存Playwrightの絶対パスを指定する。Chromeが必要。出力JSONと画像はOSの一時フォルダーへ作成し、試験用ブラウザは終了する。

### オフライン一巡（3.13.467）

同試験の `--offline` を追加し、実際のサービスワーカーと一時的なlocalhostサーバーを使用して確認した。必要資材のキャッシュ完了を確認後、ブラウザの通信を無効化しサーバーを停止。ページ再読込がサービスワーカー応答であること、未キャッシュURLへの通信が失敗することを検証したうえで、保存JSONのP0300・P0420復元、壊れたJSONのエラー表示と結果保持、戻る操作まで合格。通常試験も再実行して合格した。

ブラウザの `navigator.onLine` はこの試験環境では遮断後もtrueだったため、それだけをオフラインの証拠にしていない。通信失敗とキャッシュ応答を確認している。専用の解除済みブラウザ内でのページ再読込試験であり、PC/ブラウザの完全終了後の起動、キャッシュ退避・消失時、移行先PC・実VCIでの一巡は未確認。今回アプリ本体・送信許可は変更していない。

`deploy` で `npm run validate:r1` を実行すると、画面遷移、シリアル応答・模擬通信・接続終了、事例保存、セッション出力、オフライン資材を順番に検証し、失敗時は停止する。R1単独確認用の入口とする。模擬通信検証はシリアル検証から読み込まれるため重複実行しない。

呼び出し関係の再確認で、オフライン以外の上記検証は既に `validate:obd` に含まれていると判明した。前回の「リリース検証から抜けていた」という説明を訂正する。`validate:release` は既存のOBD検証とオフライン検証を各1回実行し、R1一式の重複実行を避ける。GitHub Actionsには未登録だったオフライン検証を追加する。

OBD全体の再実行では7446項目中5項目が失敗した。内訳は旧パスワード案内文と、通常注記から技術注記へ移動済みの4項目を要求する静的検証。実際の入力欄・情報保持を確認し、検証先を現在のUIへ合わせた。変更した5条件の単独検証と情報分離・保持の70項目は合格。診断コアの条件や検証件数を削減していない。

一括実行で、シリアル検証用の画面環境に最近追加した表示関数とモード変数が不足していたことが判明し、実際の表示関数を読み込むよう修正した。アプリ本体、診断結果、保存形式、車両送信の許可は変更していない。

確認結果: 画面遷移合格、シリアル応答1116、模擬通信304、接続終了993、事例保存157、JSON方針177、セッション出力238、オフライン165、端末版1295。各検証エラー0。これは自動回帰検証であり、実ブラウザの一巡・実車・移行先PC試験を代替しない。

## 完成形と段階公開

WindowsタブレットPCでオフライン運用する診断機を主軸とする。車両選択、VCI接続、ECU選択、読取、結果確認、保存、整備後の比較までを連続した画面遷移で扱う。G-scan ZやAutel Eliteに機能と操作性を近づけ、対応範囲は車種・ECU・機能ごとの実績で示す。VCIは通信手段であり、診断とサービス機能は本ツール側で実装する。

最初の広域目標は2021年10月までの車両への対応拡大。その後も現行車へ広げる。ただし年式だけを根拠に全車対応とは扱わず、適合表の空欄は未確認とする。使える経路から公開し、全機能完成まで試用を待たせない。

## 現在位置

| 領域 | 確認できた実装 | 未完了・未確認 |
| --- | --- | --- |
| 診断機コア | DTC、PID、FF、readiness、ECU情報、Mode06、対応PID、適合、セッション、JSON/CSV取込・保存の処理と自動検証 | 対応車種・VCI別の実車結果は自動検証と別に必要 |
| PC / ELM327 | Web Serial接続、初期化、読取順序、対応PIDに限った読取、切断・取消・異常応答の処理 | 手持ち機器のPC上のシリアル経路と実車往復の確認。Bluetooth機器すべてが対象とは限らない |
| PCオフライン | 移行用パッケージ、整合検査、起動・終了、ローカル画面とブリッジ | 移行先での一巡試験。Node.jsとVCIドライバーは別途必要 |
| iPhone / ELM327 | BLE GATTコネクター、SwiftUIホスト、読取アーカイブ、署名用ワークフローのソース | 署名済みアプリの対象端末へのインストールと実機BLE確認。未署名IPAはインストール可能な完成物ではない |
| J2534 | 登録ドライバー調査、非実行preflight、native bindingと模擬worker、終了・異常系の検証基盤 | 実ベンダーDLLとの識別試験、車両チャンネル、実transport、車種別読取の統合 |
| THINKCAR | 候補・取込側の構造 | 本ツールから手持ちVCIへ直接接続する実装と確認。公式アプリでの認識は本ツールとの互換証明にならない |
| 消去・サービス | 準備条件・無効状態の表示と保護 | 実行通信、車種別条件、終了・復旧・再読取の実装と実車検証 |

根拠: `script.js`、`obd-readonly.js`、`scripts/J2534-IDENTITY-LIFECYCLE.md`、`scripts/native/README.md`、`../native/ios/VehicleDiagnosisELMHost/README.md`。
3.13.436の前回実行結果はOBD 7446件、端末版1294件、bridge 384件、offline 165件、package 336件。これは自動検証件数であり、完成率・実車対応台数ではない。

## 公式資料との照合

参照日: 2026-09-05。以下は製品資料の観察と本ツールの設計判断を区別した計画であり、同等の対応車種や機能を実装済みという意味ではない。

- G-scan公式のZ Tab LVソフトウェア説明書では、ホーム、メーカー選択、システム検索、自己診断、データ表示などに画面が分かれる。Windows側の参考としてZ系のZ Tabを用いた。Z本体と全仕様が同一とは扱わない。
- Autel Eliteの公式製品説明を目標機能の参考とし、詳細な操作順は公開されているElite II説明書の4章を補助資料にした。車両選択からAuto ScanまたはControl Unitへ入り、ECU別の機能を選ぶ。世代差は残るためElite固有仕様と断定しない。
- 共通の参考点は、診断対象を絞ってから操作する階層構成、ECU別結果、専用のデータ画面、保存結果への導線。本ツールもこの構造を採用するが、ブランドのロゴや画面を複製しない。

資料:

- [G-scan公式説明書一覧](https://g-scan.jp/download/)
- [Z Tab LVソフトウェア説明書](https://g-scan.jp/wp-content/uploads/2024/09/ztab-software-2.pdf) 本文2、7-12ページ
- [Autel MaxiSys Elite公式製品説明](https://www.autel.com/c/www/mk3/3215.jhtml)
- [MaxiSys Elite II公式説明書](https://www.autel.com/u/cms/www/202404/01020151io71.pdf) 本文27-44、51、57ページ

## 採用する画面構造

以下は本ツールの設計方針。見た目だけでなく、この導線で実際の通信と結果確認を完結させる。

```text
ホーム
  車両診断 -> 車両選択/識別 -> 接続確認 -> 全体スキャン / ECU選択
    ECU一覧 -> ECU機能 -> DTC / データ / ECU情報 / 作動テスト / サービス
      DTC一覧 -> DTC詳細・FF -> 診断支援 / 消去前確認 -> 再読取
      データ項目選択 -> 数値・グラフ -> 記録 -> 再生・比較
  汎用OBD -> 接続確認 -> DTC / データ / FF / readiness / ECU情報
  整備サービス -> 車両・対象ECU確認 -> 適合機能 -> 条件確認 -> 実行・終了確認
  保存記録 -> セッション -> 結果・記録再生・整備前後比較・レポート
  設定 -> VCI / 表示 / オフライン更新
```

- 1画面は1つの作業目的。長いページに車両設定、結果、ロードマップを全部並べない。表と操作バーは同じ画面で扱ってよい。
- 上部に車両・ECU・接続状態、下部に戻る・ホームとその画面の操作を置く。戻っても選択と取得結果を保持する。
- タブレットのタッチ操作を優先し、主要操作は48px以上を目安にする。横画面と縦画面、小さい端末で文字切れ・重なりを検証する。
- 中立色の外枠、読みやすい明暗のデータ面、限定した状態色を使う。状態は色だけに依存させない。大きな広告風見出しや説明カードを置かない。
- ECU未応答、未実施、非対応、確認済み0件を区別する。取得時刻、単位、更新停止を表示し、古い値をライブ値と誤認させない。
- 通常画面から進捗率、内部契約、検証件数、開発トークン入力を除く。開発画面は別入口にし、画面を隠すことと実行権限の制御を混同しない。
- 故障原因の推定は取得事実の詳細から開く。推定をECUの回答と混ぜず、既存診断ロジックはUI移行のために簡略化しない。
- 未実装機能の大量の無効ボタンは並べない。適合や接続条件で使えない機能は理由を示し、開発計画は開発側で扱う。

## 開発順序の変更

旧M1-M6の直列計画を以下のR1-R5に置き換える。UIは後工程ではなく各リリースに含める。iPhoneはPCのJ2534やサービス開発を待たせる前提条件にしない。DTC消去は全メーカー対応完了を待たずに設計・実装する。

| 順序 | 利用者に届く成果 | 必須の完了条件 |
| --- | --- | --- |
| R1 基本診断機 | 通常/開発画面の分離、汎用OBDの読取・データ・保存を専用画面で操作 | 接続からDTC/FF/PID/readiness/ECU情報、保存、再読込、切断まで画面上で一巡。正常/部分取得/無応答/取消/切断を試験。オフライン再起動後も記録を開ける |
| R2 汎用OBD消去 | 消去前記録、対象と影響の確認、実行、再読取 | 対象プロトコルの要求と応答、失敗/応答不明時の扱いを検証。自動再送を避け、消去成功と修理完了を分離。対象実車での試験を経て確認済み経路を開放 |
| R3 J2534診断 | Windowsで対象ECUの読取、ECU一覧、適合に基づく全体スキャン | 実DLL識別、車両チャンネル、要求/応答、終了を統合。まず確認対象1ECU、その後複数ECU。応答の所属と時間切れを検証 |
| R4 整備サービス | 適合したEPB、ABSエア抜き、アクティブテスト等を機能単位で提供 | 車種/ECU/機能ごとの要求、条件、段階操作、終了/復旧、ログ、再確認を実装し試験。車両試験の最終判断は利用者 |
| R5 対応車拡大 | 車種別データリスト・サービス、現行車通信、整備前後比較の充実 | 出典と実測を適合表へ記録。通信可能と診断/サービス対応を別判定。機能単位の回帰試験と実車実績を更新 |

R1で数値表示・項目選択・基本グラフ・記録再生を一巡させる。高度なトリガー、長時間比較、簡易メーター装飾は後続とし、初回の利用開始を妨げない。

R3のPCドライバー接続実装はR1/R2と並行する。R4の仕様・状態遷移・模擬検証も先行し、実車実行だけを該当transportと適合確認に依存させる。サービスの着手順は資料と試験車両の揃う順で決め、EPB等を一律に低リスクとは扱わない。

## 並行・後続項目

- iPhone: 署名・インストール・BLE実装を並行して進める。手持ちELM327のBLE/Classicは未確定。公開Webページだけで直接接続可能とは案内しない。消去はPC実装だけを根拠にiPhoneでも使えるとはしない。
- THINKCAR: SDK/通信仕様/実装経路が確認できた場合に追加する。認識・取込の器を直接通信対応と呼ばない。
- オフライン配布: R1から必要資材と導入手順を揃え、移行先試験を別に記録。Node.js、ドライバー、32/64bit条件を明示。更新時に保存データを壊さず、旧版への復帰経路を検証する。
- 車種網羅: 2021年10月までの対象車をメーカー、型式、年式、ECU、機能で台帳化する。未確認を網羅済みに換算しない。優先車種は利用頻度と実証可能性で決める。
- 現行車: UDS、CAN FD、DoIP、ゲートウェイはtransport対応と車種別機能を別々に実装。正規認証やオンライン接続が必要な機能を完全オフラインと約束しない。
- ADAS、コーディング、書換え、OBD検査関連: 適合、必要機材、正規資料や制度要件を別途確認する後続領域。汎用DTC読取だけで校正や公式検査までできるとは扱わない。

## 現在位置と次の実装単位

2026-09-06現在、R1の主要操作は模擬一巡確認済み。PC配布資材の同一端末での起動・終了、試験用照合値での解除・再ロックも確認した。実機利用・別PC導入の完了とは扱わない。J2534は実transportへの工程、サービスは実行系が残る。現時点の試験件数から全体完成率や完成日を算出しない。

1. R2は `R2-DTC-CLEAR-DESIGN.md` に従い、消去前記録から結果不明・再読取までを非送信の状態管理として実装・模擬検証する。既存read-only許可リストを拡張しない。
2. R1は実機・別PCで残る確認を冒頭の表で管理する。模擬試験済みの正常系を繰り返すだけでなく、利用者評価や未確認の異常系に対応する。
3. R3のPCドライバー・transport仕様を並行して進める。R2の実車送信は対象経路・適合・条件・復旧・利用者判断が揃った個別段階で扱う。

各段階は「設計済み」「実装済み」「模擬往復確認済み」「対象実機確認済み」「配布済み」を別々に記録する。実車なしの試用は保存記録・模擬データと明記し、実機利用可能と混同しない。画面だけの完成やテスト追加だけでリリース完了にしない。

進捗報告は「今回一巡可能になった操作」「残る阻害要因」「次に公開する単位」で行う。固定の総合パーセンテージは判断材料にしない。既存の公開進捗表示の撤去/分離はR1の実装対象であり、この計画更新では表示値を変更しない。

## 変更・試験方針

### 表示整理と診断支援の改善

通常画面の静的な説明・英語の小見出し・重複見出しを減らす。通信異常、未取得、実行条件、操作直前の重大な警告は一括非表示にしない。警告ごとに常時表示/操作時表示/詳細表示を棚卸ししてから変更する。

診断支援は「大量の点検一覧」から「次に確認すべき点検と、その結果による分岐」へ改善する。R1の画面一巡を優先し、その後に取得事実と診断支援を接続する工程で扱う。既存候補は削除せず詳細に保持し、根拠不足時は識別力のある追加質問・測定へ進む。

この診断支援変更はSolでの設計・レビューを必要とする未着手項目。着手前に原因候補順位、停止条件、問診、履歴・保存互換への影響を説明する。複数DTC、矛盾する事実、情報不足、未取得、確認済み点検、重大故障の優先表示を回帰試験に含める。UI整理の名目で診断ロジックを簡略化しない。

### R1作業記録: 2026-09-05

- 3.13.447: ホームの開発・通信設定ボタンを詳細画面へ直接遷移させる修正。通常モードへ戻る操作はホームへ移動。詳細ロック・既存のシリアル切断保護・保存済みセッション保持を回帰検証。

- 公開版3.13.446のChrome実操作確認: 詳細モードでトヨタ→プリウス→ZVW30→2015→2ZR-FXE、通常モードでホンダ→N-BOXを連続選択し、車両選択画面と値の保持を確認。スクリーンショットも確認。取得できたブラウザエラーログは0件。試験入力は未選択へ、表示は元の詳細モードへ復帰。車両通信・保存は未実施。未確認だった車両選択のブラウザ確認を完了（実車確認ではない）。
- 上記実操作で見つかった次の修正対象: ホームの「開発・通信設定を開く」で詳細モードにはなるが車両選択へ着地し、「開発・詳細」を追加で押す必要がある。ボタン名に合う遷移先を検証する。

- 3.13.444: ライブ取込の計測値あり/0項目の両経路でコア進捗を内部検証欄に分離。保留・次操作・適合・未取得・空応答・レディネス・注意は通常表示を維持。既定の要約出力と分離後の全情報が一致する回帰試験を追加。実画面の確認と通常/開発画面の完全分離は未完了。

- 3.13.443: DTC取込結果の在庫比較・実機サンプル・実機応答比較・計画差分・次読取整合を「内部検証情報」へ分離。未取得、空応答、適合、保留要因、安全条件は通常表示に保持。判定と保存形式は変更なし。ライブ値側と通常/開発画面の完全分離は未完了。

- 3.13.439: ホームの保存結果入口から既存の架空native archiveを取り込み、保存DTC P0300と保留DTC P0420の表示をブラウザで確認。実車試験ではない。
- 表示確認で判明: `initializeObdStatusDisclosures` が結果更新のたびに長い注意欄を展開する。次の表示整理はエラー通知を維持しながら開発詳細を分離すること。単なる自動折りたたみで警告を隠さない。

- ローカル実装: 診断ホームから車両/接続/結果/個別データへ移動する入口を追加。既存の読取・保存・通信処理は変更していない。
- 自動検証: 専用の画面遷移テストで画面分離、ロック時非表示、セッション保持を確認。
- 続行実装: 戻る操作と開発画面からホームへの復帰を追加。開発入口をホームへ集約し、モード変更イベントをボタンだけに限定。既存のモード変更時の切断保護を維持。
- 残り: 解除後のブラウザ操作・タブレット表示確認、初期表示と戻る導線の統合、通常/開発画面の完全分離。R1完了・公開済みとはしない。

診断ロジックや保存契約の変更は、指定されたモデルでの設計・レビューと変更前の影響説明を必要とする。通常実装と表示修正は既存契約に沿って進める。pushはまとまった単位で行い、公開ページの版と実際に増えた機能を区別して報告する。

車両送信の許可はこの計画書で変更しない。状態変更機能は開発を進めるが、操作ごとの検証と利用者の実車試験判断を経る。パスワードだけで安全条件を満たしたとは扱わない。今回は文書更新のみであり、アプリ機能や安全ゲートの変更ではない。
